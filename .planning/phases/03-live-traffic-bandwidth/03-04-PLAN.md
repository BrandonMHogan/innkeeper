---
phase: 03-live-traffic-bandwidth
plan: 04
type: execute
wave: 3
depends_on: ["03-03"]
files_modified:
  - frontend/package.json
  - frontend/src/lib/api.ts
  - frontend/src/lib/components/LiveTrafficFeed.svelte
  - frontend/src/lib/components/BandwidthHistoryChart.svelte
  - frontend/src/lib/components/DestinationsBreakdown.svelte
  - frontend/src/lib/components/NetworkBandwidthChart.svelte
  - frontend/src/routes/dashboard/+page.svelte
  - frontend/src/lib/components/ui/tabs
  - frontend/src/lib/components/ui/scroll-area
autonomous: false
requirements: [TRAF-01, TRAF-02, TRAF-03, TRAF-04]
must_haves:
  truths:
    - "User can watch a live traffic feed (top talkers + active connections) update automatically every ~5-10s without refreshing the page"
    - "User can select a device and view its bandwidth history over an arbitrary time range as a chart"
    - "User can see a per-device list of domains/IPs that device has communicated with, sorted by bytes descending"
    - "User can switch between Daily, Weekly, and Monthly views of network-wide bandwidth"
    - "Byte values render as human-readable units (B/KB/MB/GB), never raw integers"
  artifacts:
    - path: "frontend/src/lib/components/LiveTrafficFeed.svelte"
      provides: "Live indicator + Top Talkers + Active Connections sections wired to GET /api/traffic/stream via native EventSource"
      contains: "EventSource"
    - path: "frontend/src/lib/components/BandwidthHistoryChart.svelte"
      provides: "Per-device bandwidth chart with time-range control, per UI-SPEC Bandwidth History section"
      contains: "LineChart"
    - path: "frontend/src/lib/components/NetworkBandwidthChart.svelte"
      provides: "Network-wide bandwidth chart with Daily/Weekly/Monthly Tabs switcher"
      contains: "Tabs"
    - path: "frontend/src/lib/components/DestinationsBreakdown.svelte"
      provides: "Per-device destinations list (domain/IP + bytes, sorted descending)"
      contains: "destinations"
  key_links:
    - from: "frontend/src/lib/components/LiveTrafficFeed.svelte"
      to: "backend GET /api/traffic/stream"
      via: "native browser EventSource, credentials included via same-origin nginx proxy"
      pattern: "EventSource.*api/traffic/stream"
    - from: "frontend/src/routes/dashboard/+page.svelte"
      to: "frontend/src/lib/components/LiveTrafficFeed.svelte"
      via: "mounted once, not torn down on device-picker/tab navigation within the traffic area (UI-SPEC Layout requirement)"
      pattern: "LiveTrafficFeed"
    - from: "frontend/src/lib/components/BandwidthHistoryChart.svelte"
      to: "backend GET /api/traffic/bandwidth/{device_id}"
      via: "apiGet with start/end query params re-fetched on device or range change"
      pattern: "bandwidth/"
---

<objective>
Build the only user-visible surface of Phase 3: a live traffic feed (top talkers + active connections, SSE-driven), a per-device bandwidth history chart with an arbitrary time-range control, a per-device destinations breakdown list, and a network-wide bandwidth chart with Daily/Weekly/Monthly tabs — exactly per 03-UI-SPEC.md's Layout, Component Inventory, Color, and Copywriting contracts, consuming the API surface Plan 03 built.

Purpose: This is the only plan in the phase a real user interacts with — without it, TRAF-01..04 remain backend-only capabilities with no observable user value.
Output: New LayerChart/Tabs/ScrollArea-based components wired to GET /api/traffic/stream (SSE) and the three GET query endpoints, integrated into the dashboard.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-live-traffic-bandwidth/03-UI-SPEC.md
@.planning/phases/03-live-traffic-bandwidth/03-CONTEXT.md
@.planning/phases/03-live-traffic-bandwidth/03-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 0: Verify layerchart package legitimacy before install</name>
  <what-built>Nothing yet — this checkpoint precedes the layerchart npm install in Task 1. Per 03-UI-SPEC.md's Registry Safety section, layerchart is a plain npm dependency (not a shadcn registry block), so the registry-vetting gate doesn't apply, but standard install hygiene still warrants a quick check given it is new to this codebase.</what-built>
  <how-to-verify>
    Visit https://www.npmjs.com/package/layerchart and confirm: (1) an active release history with a real GitHub repo link; (2) the latest published version (03-UI-SPEC.md/03-RESEARCH.md reference 1.0.13 as confirmed current at spec time — re-confirm with `npm view layerchart version` and use whatever it reports if different); (3) no suspicious postinstall scripts in the package's `package.json` (visible via the npm page's "Dependencies"/source links).
  </how-to-verify>
  <resume-signal>Type "approved" along with the exact version `npm view layerchart version` returned, to proceed with Task 1's install using that exact version.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Install dependencies, API client functions, live traffic feed</name>
  <files>frontend/package.json, frontend/src/lib/api.ts, frontend/src/lib/components/LiveTrafficFeed.svelte, frontend/src/lib/components/ui/scroll-area</files>
  <read_first>
    frontend/src/lib/api.ts (current apiGet/apiPost shape — exact pattern to extend with traffic/bandwidth fetch functions)
    frontend/src/routes/dashboard/+page.svelte (current dashboard structure — onMount auth-check pattern, inline-style convention used throughout Phase 1/2's UI, no Tailwind utility classes used yet in this file despite Tailwind being configured)
    .planning/phases/03-live-traffic-bandwidth/03-UI-SPEC.md (Color, Copywriting Contract, Component Inventory, Accessibility Contract sections — exact hex values, copy strings, and aria-live requirement for the live indicator)
  </read_first>
  <action>
    Run `npx shadcn-svelte@latest add scroll-area` to install the official `scroll-area` primitive (first-party shadcn-svelte component, exempt from registry vetting per UI-SPEC's Registry Safety table) into `frontend/src/lib/components/ui/scroll-area`.

    In `frontend/src/lib/api.ts`, add `export function openTrafficStream(): EventSource` returning `new EventSource(\`${API_BASE}/api/traffic/stream\`, { withCredentials: true })` (native browser EventSource, no library — per CLAUDE.md/RESEARCH.md's explicit "no client library needed" guidance; `withCredentials: true` is required for the session cookie to be sent, matching the existing `credentials: 'include'` convention used by `apiGet`/`apiPost`). Add `export async function getDeviceBandwidth(deviceId: number, start: string, end: string): Promise<unknown>` calling `apiGet(\`/api/traffic/bandwidth/${deviceId}?start=${encodeURIComponent(start)}&end=${encodeURIComponent(end)}\`)` and throwing on non-2xx (matching `listDevices`'s error-throw convention). Add `export async function getNetworkBandwidth(view: 'daily' | 'weekly' | 'monthly'): Promise<unknown>` calling `apiGet(\`/api/traffic/bandwidth/network?view=${view}\`)`. Add `export async function getDeviceDestinations(deviceId: number): Promise<unknown>` calling `apiGet(\`/api/traffic/devices/${deviceId}/destinations\`)`.

    Create `frontend/src/lib/components/LiveTrafficFeed.svelte`: on mount, call `openTrafficStream()` and store the `EventSource` instance in a `$state` variable; listen for the `"snapshot"` named event (`source.addEventListener('snapshot', (e) => { ... })`, parsing `e.data` as JSON into a `$state<{top_talkers: ..., active_connections: ..., computed_at: string} | null>` snapshot variable); listen for `source.onerror` to set a `connectionState: $state<'live' | 'reconnecting' | 'error'>` — native EventSource auto-reconnects on transient drops (readyState transitions to CONNECTING), so `onerror` alone cannot distinguish "reconnecting" from "terminal failure"; track consecutive error count and only flip to `'error'` after several consecutive `onerror` firings without an intervening successful snapshot (a simple heuristic: reset an error counter to 0 on every successful snapshot event, increment on each onerror, treat 3+ consecutive as terminal). On component destroy, call `source.close()`. Render per UI-SPEC's exact Layout anatomy: a heading row with the pulsing accent dot (8px, `background: var(--color-accent)`, CSS `@keyframes` opacity pulse, animation paused/dot recolored to `var(--color-muted)` when not `connectionState === 'live'`) + `Live` / `Reconnecting…` label text wrapped in a `<span aria-live="polite">` (per UI-SPEC's Accessibility Contract — this is the only aria-live element in the component; do not wrap the per-row data values) + the `Live Traffic` Heading-role text (20px/600/1.2, `var(--color-fg)`). Below that, two sections: `Top Talkers` (Label role sub-heading) rendering `snapshot.top_talkers` as a plain ordered list (device name in Label role/`--color-fg`, byte rate in Body role/`--color-muted`, formatted via a `formatBytes(n: number): string` helper you add to `frontend/src/lib/utils.ts` converting to human-readable B/KB/MB/GB per UI-SPEC's numeric convention), wrapped in the new `ScrollArea` component if more than ~6 rows; and `Active Connections` (Label role sub-heading) rendering `snapshot.active_connections` similarly (device, destination via `dst_hostname ?? dst_ip`, port/protocol in Body/muted, bytes via `formatBytes`), also wrapped in `ScrollArea`. When `snapshot` is null and `connectionState === 'live'` with zero rows in both lists, render the UI-SPEC's exact "No active connections" empty-state copy. When `connectionState === 'error'`, render the UI-SPEC's exact "Live traffic feed unavailable" error-state copy with an `AlertTriangle` icon from `@lucide/svelte` colored `var(--color-destructive)`.
  </action>
  <verify>
    <automated>cd frontend && npx svelte-check --tsconfig ./tsconfig.json 2>&1 | grep -i "LiveTrafficFeed" | grep -i error; test $? -ne 0</automated>
  </verify>
  <done>scroll-area is installed; api.ts exposes openTrafficStream/getDeviceBandwidth/getNetworkBandwidth/getDeviceDestinations; LiveTrafficFeed.svelte renders the live indicator (aria-live wrapped), top talkers, and active connections per UI-SPEC, auto-reconnects via native EventSource, and shows the specified empty/error states; svelte-check reports no type errors in the new file.</done>
</task>

<task type="auto">
  <name>Task 2: Bandwidth history chart + destinations breakdown (per-device detail view)</name>
  <files>frontend/package.json, frontend/src/lib/components/BandwidthHistoryChart.svelte, frontend/src/lib/components/DestinationsBreakdown.svelte, frontend/src/lib/utils.ts</files>
  <read_first>
    frontend/src/lib/components/DeviceCard.svelte (existing component styling conventions — inline styles using CSS custom properties, Lucide icon usage pattern)
    .planning/phases/03-live-traffic-bandwidth/03-UI-SPEC.md (Per-device bandwidth chart / Per-device destination breakdown rows in Component Inventory table; Interaction States table's device-picker/time-range-control row; Layout's "Bandwidth History [device picker]" / "Destinations" anatomy block)
  </read_first>
  <action>
    Run `npm install layerchart` at the version confirmed in the checkpoint above, added to `frontend/package.json` dependencies.

    Add `formatBytes(n: number): string` to `frontend/src/lib/utils.ts` if not already added in Task 1 (B/KB/MB/GB thresholds at powers of 1024, one decimal place, e.g. `1468006` → `"1.4 MB"` — this is the single shared formatter both this task's components and Task 1's LiveTrafficFeed must use; if Task 1 already created it, import and reuse it here rather than duplicating).

    Create `frontend/src/lib/components/BandwidthHistoryChart.svelte` accepting a `deviceId: number` prop. Maintain `$state` for `start`/`end` (default to "last 24h" per UI-SPEC's Interaction States table — exact default is implementation discretion, last-24h is a sensible one) and `points: {time: string, bytes_rx: number, bytes_tx: number}[]`. On mount and whenever `deviceId`/`start`/`end` change (via `$effect`), call `getDeviceBandwidth(deviceId, start, end)` and populate `points`; show a centered `Loading…` (Body/muted) text while the request is in flight, and the UI-SPEC's exact "No bandwidth data yet" empty-state copy when `points` is empty after a successful load, or the "Couldn't load bandwidth history" error-state copy (with `AlertTriangle`) on a request failure. Render a `LineChart` (or `AreaChart`) from `layerchart` with a single accent-colored (`var(--color-accent)`) series plotting `bytes_rx + bytes_tx` (or both as two visually-distinguishable lines if layerchart's API makes that straightforward — UI-SPEC only mandates "single accent-colored series" as the baseline contract, not a hard prohibition on showing rx/tx separately, but if in doubt default to the single combined series the UI-SPEC explicitly describes), wrapped in the existing `Card` component (`frontend/src/lib/components/ui/card`), with a simple time-range control above it (two date/datetime `Input` fields bound to `start`/`end`, reusing the existing `ui/input` component — exact widget choice is executor discretion per UI-SPEC's Layout note).

    Create `frontend/src/lib/components/DestinationsBreakdown.svelte` accepting a `deviceId: number` prop. On mount and whenever `deviceId` changes, call `getDeviceDestinations(deviceId)` and render the returned `destinations` array as a plain repeating-row list (label in Body/`--color-fg`, `formatBytes(bytes)` in Body/`--color-muted`), already sorted descending by the API (no client-side re-sort needed) — wrapped in `Card`, with the UI-SPEC's exact "No destinations recorded" empty-state copy when the array is empty.
  </action>
  <verify>
    <automated>cd frontend && npx svelte-check --tsconfig ./tsconfig.json 2>&1 | grep -iE "BandwidthHistoryChart|DestinationsBreakdown" | grep -i error; test $? -ne 0</automated>
  </verify>
  <done>layerchart is installed; BandwidthHistoryChart fetches and renders a per-device chart over a user-adjustable arbitrary time range with loading/empty/error states; DestinationsBreakdown renders the registered-domain/IP byte breakdown sorted descending; both use formatBytes for all byte values; svelte-check reports no type errors in either new file.</done>
</task>

<task type="auto">
  <name>Task 3: Network-wide bandwidth chart, dashboard wiring, and device-picker integration</name>
  <files>frontend/src/lib/components/NetworkBandwidthChart.svelte, frontend/src/lib/components/ui/tabs, frontend/src/routes/dashboard/+page.svelte</files>
  <read_first>
    frontend/src/routes/dashboard/+page.svelte (current full file — current state/onMount structure to extend, not replace; registeredDevices derived value already exists and is the natural source for a device picker)
    .planning/phases/03-live-traffic-bandwidth/03-UI-SPEC.md (Layout's full page anatomy diagram — section order: Live Traffic, then Bandwidth History + device picker + Destinations, then Network-Wide Bandwidth with Daily/Weekly/Monthly tabs; Layout's explicit requirement that the live feed's SSE connection must not be torn down by device-picker/tab navigation within the traffic area)
  </read_first>
  <action>
    Run `npx shadcn-svelte@latest add tabs` to install the official `tabs` primitive into `frontend/src/lib/components/ui/tabs`.

    Create `frontend/src/lib/components/NetworkBandwidthChart.svelte`: `$state` for `view: 'daily' | 'weekly' | 'monthly'` defaulting to `'daily'`, and `buckets: {bucket: string, bytes_rx: number, bytes_tx: number}[]`. On mount and whenever `view` changes, call `getNetworkBandwidth(view)` and populate `buckets`; same loading/empty("No network activity recorded yet")/error("Couldn't load bandwidth history" — reuse the same error copy string as BandwidthHistoryChart since UI-SPEC defines one generic chart-query-failure message) state handling as Task 2's chart. Render the `Tabs` component (from `ui/tabs`) with three `TabsTrigger`s labeled exactly `Daily` / `Weekly` / `Monthly` (per UI-SPEC's Copywriting Contract) bound to `view`, above a `BarChart` or `AreaChart` from `layerchart` plotting `bytes_rx + bytes_tx` per bucket in `var(--color-accent)`, wrapped in `Card`.

    In `frontend/src/routes/dashboard/+page.svelte`: import and mount `LiveTrafficFeed` once, near the top of the authenticated `<main>` block, above the existing device-card grid section heading structure — per UI-SPEC's Layout, "Live Traffic" is the primary focal point and appears first. Add a `selectedDeviceId: $state<number | null>` (default to the first registered device, or null if none registered yet) driving both `BandwidthHistoryChart` and `DestinationsBreakdown`, rendered together in a "Bandwidth History" + "Destinations" section below the live feed, with a simple device-picker `<select>` (bound to `selectedDeviceId`, options from the existing `registeredDevices` derived array) above both — selecting a different device re-renders both child components via their `deviceId` prop changing, without remounting `LiveTrafficFeed` (place `LiveTrafficFeed` outside/above the device-picker-scoped conditional block so its mount lifecycle is independent of `selectedDeviceId` changes, per UI-SPEC's explicit "SSE connection does not get torn down by unrelated navigation" requirement). Mount `NetworkBandwidthChart` once, last, in its own "Network-Wide Bandwidth" section below Bandwidth History/Destinations. Use the existing inline-style convention already present in this file (CSS custom properties via `style="..."` attributes, matching the Heading-role 20px/600/1.2 styling already used for "Dashboard") for all new section headings ("Live Traffic", "Bandwidth History", "Network-Wide Bandwidth"), and the `lg`(24px)/`2xl`(48px) spacing per UI-SPEC's Spacing Scale between sections.
  </action>
  <verify>
    <automated>cd frontend && npx svelte-check --tsconfig ./tsconfig.json 2>&1 | grep -iE "NetworkBandwidthChart|dashboard" | grep -i error; test $? -ne 0</automated>
  </verify>
  <done>tabs is installed; NetworkBandwidthChart switches between Daily/Weekly/Monthly views re-querying the network endpoint on tab change; dashboard mounts LiveTrafficFeed (independent lifecycle from device selection), a device-picker-driven Bandwidth History + Destinations section, and NetworkBandwidthChart in the UI-SPEC's specified section order; svelte-check reports no type errors in the modified/new files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| browser → GET /api/traffic/stream, /bandwidth/*, /devices/*/destinations | Same-origin (nginx reverse-proxy per existing Phase 1 convention), session-cookie authenticated — no new trust boundary, reuses the existing same-origin + SameSite=Lax cookie architecture |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-03-11 | Information Disclosure | LiveTrafficFeed renders all devices' live traffic in one global view | accept | Matches the backend's accepted disposition (T-03-09) — single-user/household model, no per-device UI-level authorization needed |
| T-03-12 | Denial of Service | Unbounded active_connections list rendering on the client if the backend ever returns an unexpectedly large snapshot | mitigate | ScrollArea + the backend's own snapshot cap (T-03-08's payload-size bound from Plan 03) together bound both data volume and DOM node count; no additional client-side truncation required beyond what Plan 03 already caps server-side |
| T-03-SC | Tampering | npm installs (layerchart) | mitigate | Blocking human-verify checkpoint (Task above) precedes the layerchart install per the package-legitimacy gate protocol |

</threat_model>

<verification>
- `cd frontend && npx svelte-check --tsconfig ./tsconfig.json` reports no new type errors introduced by this plan's files
- Manual (per 03-VALIDATION.md — no frontend test framework exists yet): open dashboard, generate traffic on a test device, confirm live feed/top-talkers update every ~5-10s without manual refresh; switch device picker and confirm bandwidth chart/destinations re-query without disrupting the live feed; switch Daily/Weekly/Monthly tabs and confirm the network chart re-renders; kill the backend mid-session and confirm the browser EventSource auto-reconnects and the live feed resumes
</verification>

<success_criteria>
- Live traffic feed updates automatically via SSE without page refresh, with correct Live/Reconnecting/Error states
- Per-device bandwidth chart supports an arbitrary, user-adjustable time range
- Per-device destinations list shows registered-domain-grouped entries sorted by bytes descending
- Network-wide chart supports Daily/Weekly/Monthly views via tabs
- All byte values render as human-readable units
- Live feed's SSE connection survives device-picker and tab navigation within the page
</success_criteria>

<output>
Create `.planning/phases/03-live-traffic-bandwidth/03-04-SUMMARY.md` when done
</output>
