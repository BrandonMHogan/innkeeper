---
phase: 03-live-traffic-bandwidth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/models/traffic_flow.py
  - backend/src/models/device_mac_history.py
  - backend/src/services/bandwidth_source.py
  - backend/src/services/domain_grouping.py
  - backend/src/services/discovery.py
  - backend/alembic/versions/0004_traffic_flows.py
  - backend/pyproject.toml
  - backend/tests/test_domain_grouping.py
  - backend/tests/test_mac_history.py
autonomous: true
requirements: [TRAF-02, TRAF-03, TRAF-04]
must_haves:
  truths:
    - "A traffic_flows row can be written for a (time, device_mac, dst_ip, dst_port, protocol) 5-tuple and queried back"
    - "A device's bandwidth/traffic history is queryable across every MAC it has ever used, not just its current last_known_mac"
    - "A raw hostname like www.netflix.com groups under netflix.com; foo.github.io groups under github.io (multi-part PSL suffix handled correctly)"
    - "bandwidth_metrics and traffic_flows are never deleted by any code path introduced this phase (compression policy only, no retention policy)"
  artifacts:
    - path: "backend/src/models/traffic_flow.py"
      provides: "TrafficFlow SQLAlchemy model for the traffic_flows hypertable"
      contains: "class TrafficFlow(Base)"
    - path: "backend/src/models/device_mac_history.py"
      provides: "DeviceMacHistory model tracking every MAC a Device has ever used"
      contains: "class DeviceMacHistory(Base)"
    - path: "backend/src/services/bandwidth_source.py"
      provides: "Swappable BandwidthSource Protocol + PassiveCaptureBandwidthSource implementation (D-07)"
      contains: "class BandwidthSource(Protocol)"
    - path: "backend/src/services/domain_grouping.py"
      provides: "tldextract-based registered-domain grouping (D-10), offline mode only"
      contains: "def registered_domain"
    - path: "backend/alembic/versions/0004_traffic_flows.py"
      provides: "traffic_flows hypertable + device_mac_history table + continuous aggregates + compression policy, no retention policy"
      contains: "create_hypertable"
  key_links:
    - from: "backend/src/services/discovery.py"
      to: "backend/src/models/device_mac_history.py"
      via: "record_observation's Device-branch fast path inserts/updates a DeviceMacHistory row whenever last_known_mac changes"
      pattern: "DeviceMacHistory"
    - from: "backend/src/services/domain_grouping.py"
      to: "tldextract.TLDExtract"
      via: "module-level offline-mode extractor instance, suffix_list_urls=()"
      pattern: "suffix_list_urls=\\(\\)"
---

<objective>
Build the storage and service foundation Phase 3's traffic/bandwidth pipeline rests on: the new `traffic_flows` TimescaleDB hypertable (D-05), a `device_mac_history` table that closes the MAC-rotation blind spot identified in 03-RESEARCH.md Pitfall 1 / Open Question 1, the swappable `BandwidthSource` interface (D-07) that the ingest route (Plan 03) will write through, and the registered-domain grouping service (D-10) the destinations endpoint (Plan 03) will call at serialization time.

Purpose: Every other Phase 3 plan (capture sniffer, ingest route, SSE/query routes, frontend) depends on this schema and these services existing first — this plan is the dependency root of the phase.
Output: `traffic_flows` + `device_mac_history` tables (via Alembic migration with continuous aggregates and compression, no retention policy), `TrafficFlow`/`DeviceMacHistory` models, `BandwidthSource` Protocol + concrete passive-capture implementation, `registered_domain()` grouping function, and `record_observation` wired to populate MAC history.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-live-traffic-bandwidth/03-CONTEXT.md
@.planning/phases/03-live-traffic-bandwidth/03-RESEARCH.md
@.planning/phases/03-live-traffic-bandwidth/03-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: traffic_flows + device_mac_history schema (model + migration)</name>
  <files>backend/src/models/traffic_flow.py, backend/src/models/device_mac_history.py, backend/alembic/versions/0004_traffic_flows.py, backend/tests/test_mac_history.py</files>
  <read_first>
    backend/src/models/bandwidth.py (exact structural analog — composite PK on time+device_mac, Float byte columns)
    backend/src/models/device.py (Device.last_known_mac, Device.id — device_mac_history.device_id FKs here)
    backend/alembic/versions/0001_initial.py (create_hypertable + CREATE INDEX IF NOT EXISTS shape to copy)
    backend/alembic/versions/0003_identification_hints.py (confirms current migration head is revision "0003")
    backend/src/services/discovery.py (record_observation — Task 2 modifies this; read now to understand the Device-branch fast path you'll be hooking into)
  </read_first>
  <behavior>
    - Test 1: Inserting two DeviceMacHistory rows for the same device_id with different macs, then querying all macs for that device_id, returns both macs.
    - Test 2: Inserting a DeviceMacHistory row, then inserting another row for the same (device_id, mac) pair with a later seen_at, does not raise an integrity error (upsert-or-allow-multiple-rows — composite PK is (device_id, mac), so an update path is needed, not a second insert).
  </behavior>
  <action>
    Create `backend/src/models/traffic_flow.py` with `class TrafficFlow(Base)`, `__tablename__ = "traffic_flows"`: columns `time` (DateTime(timezone=True), nullable=False, server_default=func.now(), primary_key=True), `device_mac` (String(17), nullable=False, primary_key=True), `dst_ip` (String(45), nullable=False, primary_key=True), `dst_port` (Integer, nullable=True, primary_key=True), `protocol` (Integer, nullable=False, primary_key=True), `bytes` (Float, nullable=False, default=0.0), `dst_hostname` (String(255), nullable=True) — dst_hostname stores the raw DNS-sniffed hostname (D-08), never the grouped registered domain (D-10 grouping happens at query time only, in domain_grouping.py).

    Create `backend/src/models/device_mac_history.py` with `class DeviceMacHistory(Base)`, `__tablename__ = "device_mac_history"`: columns `device_id` (Integer, ForeignKey("devices.id"), nullable=False, primary_key=True), `mac` (String(17), nullable=False, primary_key=True), `first_seen` (DateTime(timezone=True), nullable=False, server_default=func.now()), `last_seen` (DateTime(timezone=True), nullable=False, server_default=func.now()). This is the table that closes 03-RESEARCH.md Pitfall 1 — every MAC a Device has ever had gets a row here, so historical bandwidth/traffic_flows queries can join across all of a device's MACs, not just `Device.last_known_mac`.

    Create `backend/alembic/versions/0004_traffic_flows.py` with `revision = "0004"`, `down_revision = "0003"`. In `upgrade()`: `op.create_table("traffic_flows", ...)` matching the TrafficFlow model's columns and composite PK; `op.execute("SELECT create_hypertable('traffic_flows', by_range('time', INTERVAL '1 week'))")`; `op.execute("CREATE INDEX IF NOT EXISTS traffic_flows_time_idx ON traffic_flows (time)")`. Then `op.create_table("device_mac_history", ...)` matching the DeviceMacHistory model (plain Postgres table, not a hypertable — it's a small lookup table, not time-series). Then add continuous aggregates per 03-RESEARCH.md's Code Examples block: `bandwidth_hourly` (`CREATE MATERIALIZED VIEW ... WITH (timescaledb.continuous) AS SELECT time_bucket('1 hour', time) AS bucket, device_mac, sum(bytes_rx) AS bytes_rx, sum(bytes_tx) AS bytes_tx FROM bandwidth_metrics GROUP BY bucket, device_mac WITH NO DATA`), `bandwidth_daily` (built on `bandwidth_hourly`), `bandwidth_weekly` and `bandwidth_monthly` (built on `bandwidth_daily`) — use `add_continuous_aggregate_policy` for each with `start_offset`/`end_offset`/`schedule_interval` scaled to the bucket size (hourly: start_offset 3 hours/schedule 30 min; daily: start_offset 3 days/schedule 1 hour; weekly: start_offset 3 weeks/schedule 6 hours; monthly: start_offset 3 months/schedule 1 day). Add `ALTER TABLE bandwidth_metrics SET (timescaledb.compress, timescaledb.compress_segmentby = 'device_mac', timescaledb.compress_orderby = 'time DESC')` and `SELECT add_compression_policy('bandwidth_metrics', compress_after => INTERVAL '7 days')`; repeat the same compress/compression-policy pair for `traffic_flows` (compress_segmentby = 'device_mac'). Do NOT call `add_retention_policy` anywhere in this migration (D-06 — never auto-delete by default). In `downgrade()`: drop the continuous aggregates (`DROP MATERIALIZED VIEW IF EXISTS bandwidth_monthly`, etc., in reverse dependency order), then `op.drop_table("device_mac_history")`, then `op.drop_table("traffic_flows")`.

    Create `backend/tests/test_mac_history.py` with the two behavior tests above, using the `test_db`/`client` fixtures pattern from `backend/tests/conftest.py` (note: continuous aggregates/hypertable SQL only runs via Alembic against real Postgres, never via `Base.metadata.create_all` against the SQLite test fixture — same established pattern as `bandwidth_metrics`, so these tests exercise plain CRUD on `DeviceMacHistory` via SQLAlchemy, not the hypertable-specific SQL).
  </action>
  <verify>
    <automated>cd backend && pytest tests/test_mac_history.py -x</automated>
  </verify>
  <done>TrafficFlow and DeviceMacHistory models exist with the specified columns; migration 0004 creates both tables plus 4 continuous aggregates and 2 compression policies with zero retention-policy calls; test_mac_history.py passes against the SQLite fixture.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MAC-history wiring, swappable bandwidth source, domain grouping</name>
  <files>backend/src/services/discovery.py, backend/src/services/bandwidth_source.py, backend/src/services/domain_grouping.py, backend/pyproject.toml, backend/tests/test_domain_grouping.py</files>
  <read_first>
    backend/src/services/discovery.py (record_observation — the exact Device-branch fast path to modify; current state has no MAC-history write)
    backend/src/services/identity_resolver.py (Protocol + concrete-class pattern to replicate for BandwidthSource)
    backend/src/models/device_mac_history.py (created in Task 1 — read after Task 1 completes within this same plan)
  </read_first>
  <behavior>
    - Test 1: `registered_domain("www.netflix.com")` returns `"netflix.com"`.
    - Test 2: `registered_domain("api.netflix.com")` returns `"netflix.com"` (same registered domain as Test 1, proving subdomain grouping per D-10).
    - Test 3: `registered_domain("foo.github.io")` returns `"github.io"` (multi-part public-suffix-list exception handled — proves tldextract offline mode loaded a real PSL snapshot, not a naive last-two-labels heuristic).
    - Test 4: `registered_domain("17.253.1.1")` (a bare IP, no hostname available) returns the input unchanged — proves the IP-fallback path (D-09) doesn't crash or mangle an IP string.
  </behavior>
  <action>
    Add `tldextract==5.3.0` to `backend/pyproject.toml`'s `dependencies` list (verify exact current version with `pip index versions tldextract` before pinning, per 03-RESEARCH.md A2 — use whatever that command reports as latest if it differs from 5.3.0).

    Create `backend/src/services/domain_grouping.py`: module-level `_extractor = tldextract.TLDExtract(suffix_list_urls=())` (offline mode, zero outbound network calls per D-09/CLAUDE.md's no-telemetry constraint), and `def registered_domain(hostname: str) -> str` that calls `_extractor(hostname)` and returns `f"{ext.domain}.{ext.suffix}"` when `ext.suffix` is truthy, else returns `hostname` unchanged (covers both the bare-IP case and any malformed/unparseable hostname). Keep this as a pure, stateless module-level function — no class, no constructor args, matching `HostnameFallbackResolver`'s no-I/O style. Called only at query/serialization time (Plan 03's destinations endpoint), never at capture/ingest time — `TrafficFlow.dst_hostname` always stores the raw, ungrouped hostname.

    Create `backend/src/services/bandwidth_source.py` implementing D-07's swappable interface using the exact `Protocol` + single concrete class shape from `identity_resolver.py`: `class BandwidthSource(Protocol)` with one async method `async def write_rollup(self, db: AsyncSession, device_mac: str, bytes_rx: float, bytes_tx: float, observed_at: datetime) -> None: ...`, and `class PassiveCaptureBandwidthSource` implementing it by constructing and adding a `BandwidthMetric` row to the session (import `BandwidthMetric` from `src.models.bandwidth`) and committing. Docstring on `PassiveCaptureBandwidthSource` must state it is the only source today and that Phase 7's UniFi adapter will implement the same `BandwidthSource` Protocol without requiring changes to callers (this is the swappability D-07 requires) — Plan 03's ingest route will call this through a `resolver: "BandwidthSource | None" = None`-style default-injection parameter, same idiom as `record_observation`'s `resolver` parameter.

    Modify `backend/src/services/discovery.py`'s `record_observation`: in the existing Device-branch fast path (the `if device is not None:` block that already updates `device.identity_key`, `device.last_known_mac`, `device.last_seen`), after `device.last_known_mac = observation.mac`, add logic that upserts a `DeviceMacHistory` row for `(device.id, observation.mac)` — if a row for this `(device_id, mac)` pair already exists, update its `last_seen`; otherwise insert a new row with `first_seen=last_seen=observation.observed_at`. Use the same dialect-aware `pg_insert`/`sqlite_insert` `.on_conflict_do_update()` pattern already used in this file's `upsert_discovered_identity` function (copy that pattern's dialect-branching shape, targeting `DeviceMacHistory`'s composite PK `(device_id, mac)` as the conflict target) — do not introduce a third upsert style into this file. Import `DeviceMacHistory` from `src.models.device_mac_history`.

    Create `backend/tests/test_domain_grouping.py` with the four behavior tests above as plain (non-async) `def test_...()` functions — no fixtures needed, matching `HostnameFallbackResolver`'s pure-function test style described in 03-PATTERNS.md.
  </action>
  <verify>
    <automated>cd backend && pytest tests/test_domain_grouping.py tests/test_mac_history.py -x</automated>
  </verify>
  <done>registered_domain() correctly groups subdomains under their registered domain including multi-part PSL suffixes, and passes through unparseable/IP input unchanged; record_observation's Device-branch fast path writes/updates a DeviceMacHistory row on every MAC update; BandwidthSource Protocol + PassiveCaptureBandwidthSource exist and are importable; all four new/modified test files pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| capture container → API ingest route | Out of scope for this plan (Plan 03 builds the ingest route) — this plan only builds the storage/service layer the ingest route will call into |
| API query layer → TimescaleDB | Internal, same-process SQLAlchemy session — no new external trust boundary introduced by this plan |
| domain_grouping.py → tldextract | Offline-only PSL lookup, zero outbound network calls (suffix_list_urls=()) — no network trust boundary exists |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-03-01 | Tampering | Alembic migration 0004 (continuous aggregates/compression SQL) | mitigate | Migration runs only via `alembic upgrade head` in the trusted API container's startup command (existing docker-compose pattern); no user-controllable input reaches this SQL |
| T-03-02 | Information Disclosure | device_mac_history table (stores every MAC a device has used) | accept | Same exposure profile as existing `Device.last_known_mac` — single-user/household threat model already accepts MAC visibility behind the dashboard auth gate; this table is read only by Plan 03's authenticated query routes |
| T-03-03 | Denial of Service | Continuous aggregate refresh policies (4 caggs, scheduled refreshes) | accept | Refresh intervals (30min/1h/6h/1d) are conservative and match TimescaleDB's documented defaults for this data volume; no user-facing endpoint triggers refreshes synchronously |
| T-03-SC | Tampering | npm/pip installs (tldextract) | mitigate | tldextract already audited in 03-RESEARCH.md's Package Legitimacy Audit table (verdict: Approved, flagged SUS only due to unknown-downloads signal gap, not a real risk) — no additional checkpoint required per that audit's disposition; version re-verified via `pip index versions tldextract` at install time per A2 |

</threat_model>

<verification>
- `cd backend && pytest tests/test_mac_history.py tests/test_domain_grouping.py -x` passes
- `cd backend && pytest` (full suite) passes — confirms no regression to Phase 1/2 tests
- Manual: `grep -c "add_retention_policy" backend/alembic/versions/0004_traffic_flows.py` returns 0 (confirms D-06 compliance)
</verification>

<success_criteria>
- traffic_flows hypertable, device_mac_history table, 4 continuous aggregates, and 2 compression policies exist via migration 0004
- TrafficFlow, DeviceMacHistory models importable and match migration schema
- registered_domain() handles subdomain grouping (including multi-part PSL suffixes) and IP fallback correctly
- record_observation populates device_mac_history on every Device-branch MAC update, closing the MAC-rotation blind spot (03-RESEARCH.md Pitfall 1)
- BandwidthSource Protocol + PassiveCaptureBandwidthSource exist, ready for Plan 03's ingest route to consume
</success_criteria>

<output>
Create `.planning/phases/03-live-traffic-bandwidth/03-01-SUMMARY.md` when done
</output>
