---
phase: 04-security
plan: 04
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - frontend/src/lib/api.ts
  - frontend/src/lib/components/DeviceCard.svelte
  - frontend/src/lib/components/ScanResultDialog.svelte
  - frontend/src/lib/components/SecurityAlertsBanner.svelte
  - frontend/src/lib/styles/theme.css
  - frontend/src/routes/dashboard/+page.svelte
  - frontend/components.json
autonomous: false
requirements: [SEC-01, SEC-02, SEC-03, SEC-04]

must_haves:
  truths:
    - "Every device card shows a good/warning/critical security badge with a text label, never color-only"
    - "User can click Scan (or Re-scan) on a registered device card, see a Scanning… disabled state, and the result becomes visible without a manual page refresh"
    - "User can open a scan-result panel showing each open port with its risky/unexpected/expected flag and the common service name"
    - "Dashboard shows a dismissible Security Alerts banner above the existing summary banner when unacknowledged alerts exist, and nothing when none exist"
    - "User can dismiss a single alert immediately, or dismiss all with a confirmation step"
  artifacts:
    - path: "frontend/src/lib/components/ScanResultDialog.svelte"
      provides: "Dialog showing scan result port list with risky/unexpected/expected flags per 04-UI-SPEC.md Copywriting Contract"
      contains: "Scan Results"
    - path: "frontend/src/lib/components/SecurityAlertsBanner.svelte"
      provides: "Dashboard alerts banner reading /api/security/alerts, per-row dismiss, dismiss-all with AlertDialog confirmation"
      contains: "Security Alerts"
    - path: "frontend/src/lib/styles/theme.css"
      provides: "New --color-good CSS token (green-500, #22c55e) per 04-UI-SPEC.md Color section"
      contains: "--color-good"
  key_links:
    - from: "frontend/src/lib/components/DeviceCard.svelte"
      to: "frontend/src/lib/api.ts"
      via: "triggerScan(deviceId) call on Scan/Re-scan button click"
      pattern: "triggerScan\\("
    - from: "frontend/src/lib/components/SecurityAlertsBanner.svelte"
      to: "frontend/src/lib/api.ts"
      via: "listAlerts()/ackAlert(id)/ackAllAlerts() calls"
      pattern: "listAlerts\\(\\)"
    - from: "frontend/src/routes/dashboard/+page.svelte"
      to: "frontend/src/lib/components/SecurityAlertsBanner.svelte"
      via: "rendered above the existing Phase 2 summary banner, per D-12/UI-SPEC Layout"
      pattern: "SecurityAlertsBanner"
---

<objective>
Build Phase 4's frontend surfaces per 04-UI-SPEC.md: a good/warning/critical security badge and Scan/Re-scan button on every `DeviceCard`, a new `ScanResultDialog` showing the flagged-port breakdown, and a new `SecurityAlertsBanner` above the dashboard's existing Phase 2 summary banner. Introduces the project's first `--color-good` token and first `tooltip`/`alert`/`alert-dialog` shadcn-svelte component installs, per the UI-SPEC's Component Inventory.

Purpose: Closes SEC-01/02/03/04's user-facing surfaces — without this plan, the backend's fully-tested detection/classification logic (Plans 04-01/04-02) is invisible to the user.
Output: 2 new Svelte components, `DeviceCard.svelte` extended, `theme.css` gains `--color-good`, `api.ts` gains 5 new functions, dashboard page wires the new banner, 3 new shadcn-svelte components installed (`tooltip`, `alert`, `alert-dialog`).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-security/04-CONTEXT.md
@.planning/phases/04-security/04-UI-SPEC.md
@.planning/phases/04-security/04-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install new shadcn-svelte components, add --color-good token, extend api.ts</name>
  <files>frontend/components.json, frontend/src/lib/styles/theme.css, frontend/src/lib/api.ts</files>
  <read_first>
    frontend/src/lib/styles/theme.css (current full file — the two-layer brand-alias + shadcn-token convention; add the new token following the exact same pattern as the Phase 2 --color-warning addition)
    frontend/src/lib/api.ts (current full file — apiGet/apiPost helper shape, existing function naming/return-type conventions)
    frontend/components.json (confirms shadcn-svelte registry config — official registry only, no third-party)
  </read_first>
  <action>
    Run the shadcn-svelte CLI to add three official components: `tooltip`, `alert`, `alert-dialog` (e.g. `npx shadcn-svelte@latest add tooltip alert alert-dialog` from the `frontend/` directory, or the project's existing equivalent invocation — match whatever installation method the existing `badge`/`dialog`/`popover` components were installed with, inferred from `frontend/src/lib/components/ui/` directory structure). These populate `frontend/src/lib/components/ui/tooltip/`, `frontend/src/lib/components/ui/alert/`, `frontend/src/lib/components/ui/alert-dialog/`.

    Modify frontend/src/lib/styles/theme.css: add a new semantic color token in `:root`, directly below the existing `--color-warning`/`--color-warning-fg` pair (same placement style as that Phase 2 addition): `--color-good: #22c55e;` with a one-line comment `/* Phase 4 semantic addition: security-status good (green-500, distinct from --color-accent's online/live teal-green) */`. Per UI-SPEC's Color section, `--color-good` is used ONLY for the security badge's "Good" state — do not map it into the `.dark`/`@theme inline` shadcn-token blocks (it is a brand-alias-only token, consumed directly via inline `style=` attributes exactly like `--color-warning` already is on the existing "Unknown" badge).

    Modify frontend/src/lib/api.ts: add five new functions following the exact `apiGet`/`apiPost` wrapper convention already used by every existing function in this file —
    - `triggerScan(deviceId: number): Promise<Response>` — `return apiPost(\`/api/security/scan/${deviceId}\`, {})`.
    - `listAlerts(): Promise<unknown[]>` — `apiGet('/api/security/alerts')`, throws on non-ok, returns `res.json()`.
    - `ackAlert(alertId: number): Promise<Response>` — `return apiPost(\`/api/security/alerts/${alertId}/ack\`, {})`.
    - `ackAllAlerts(): Promise<Response>` — `return apiPost('/api/security/alerts/ack-all', {})`.
    - `getScanResult(deviceId: number): Promise<unknown>` — NOT a new backend route from Plan 04-02; instead derive the latest scan result from the existing `GET /api/devices` payload's `security_status`/`last_scanned_at` fields already present per Plan 04-02 — skip creating this function. Only implement the four functions above.
  </action>
  <verify>
    <automated>cd frontend && npx svelte-check --tsconfig ./tsconfig.json 2>&1 | tail -30</automated>
  </verify>
  <done>frontend/src/lib/components/ui/tooltip/, alert/, alert-dialog/ directories exist; theme.css has a --color-good: #22c55e entry; api.ts exports triggerScan, listAlerts, ackAlert, ackAllAlerts matching the existing apiGet/apiPost wrapper style; svelte-check reports no new type errors introduced by this task's changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DeviceCard badge + Scan button, ScanResultDialog, SecurityAlertsBanner, dashboard wiring</name>
  <files>frontend/src/lib/components/DeviceCard.svelte, frontend/src/lib/components/ScanResultDialog.svelte, frontend/src/lib/components/SecurityAlertsBanner.svelte, frontend/src/routes/dashboard/+page.svelte</files>
  <read_first>
    frontend/src/lib/components/DeviceCard.svelte (current full file — the existing "Unknown" Badge pattern at lines ~116-118, the online-status dot pattern, the registered-device CardContent branch which currently has no action buttons; modify in place)
    frontend/src/routes/dashboard/+page.svelte (current full file — existing summary-banner inline div, device-grid placement, onMount/loadDevices pattern; modify in place to insert SecurityAlertsBanner)
    frontend/src/lib/components/RegisterDialog.svelte (Dialog/DialogContent/DialogHeader/DialogTitle usage pattern to replicate in ScanResultDialog)
    .planning/phases/04-security/04-UI-SPEC.md (Copywriting Contract section — exact badge/button/dialog copy strings; Layout section — banner stacking order; Interaction States section — scan button states)
  </read_first>
  <behavior>
    - Test 1: a registered device with security_status="good" renders a Badge with visible text "Good" colored via --color-good
    - Test 2: a registered device with security_status="warning" renders a Badge with visible text "Warning" colored via --color-warning
    - Test 3: a registered device with security_status="critical" renders a Badge with visible text "Critical" colored via --color-destructive
    - Test 4: a registered device with last_scanned_at=null shows a "Scan" button and a "Not yet scanned" label near the badge; a device with a non-null last_scanned_at shows a "Re-scan" button and a "Last scanned {relative time}" label
    - Test 5: clicking Scan/Re-scan calls triggerScan(device.id), the button becomes disabled and reads "Scanning…" immediately, and the click handler is wrapped so a thrown/rejected triggerScan call restores the button to its prior label and shows an inline error (per UI-SPEC's Error state copy: "Scan couldn't complete")
    - Test 6: SecurityAlertsBanner renders nothing (no DOM output) when listAlerts() resolves to an empty array
    - Test 7: SecurityAlertsBanner renders a "Security Alerts" heading and one row per alert when listAlerts() resolves to a non-empty array, with each row's message text matching the Copywriting Contract per alert type (e.g. type=unknown_device renders "Unknown device joined the network")
    - Test 8: clicking a single alert row's dismiss (×) calls ackAlert(alert.id) and removes that row from the list without a confirmation dialog
    - Test 9: clicking "Dismiss all" (only rendered when 2+ alerts exist) opens an AlertDialog confirmation; confirming calls ackAllAlerts() and clears the banner
  </behavior>
  <action>
    Modify frontend/src/lib/components/DeviceCard.svelte: extend the `DeviceCardDevice` interface with `security_status?: string | null`, `last_scanned_at?: string | null`. Add a new prop `onScan: (deviceId: number) => void` alongside the existing `onRegister`/`onMerge` props (component signature becomes `{ device, onRegister, onMerge, onScan }`). In the registered-device branch (the `{:else}` `<Card>` block, currently has no action-button row), add: (1) a security badge using the exact `<Badge variant="outline" style="color: var(--color-X); border-color: var(--color-X);">` shape already used for the "Unknown" badge, where X is `--color-good`/`--color-warning`/`--color-destructive` keyed off `device.security_status` (default to good/`--color-good` if `security_status` is null/undefined, matching D-06's never-scanned-defaults-to-good rule), with the label text `Good`/`Warning`/`Critical` per the Copywriting Contract — placed in the `CardHeader` row, next to the existing online-status dot (not replacing it — both indicators coexist per UI-SPEC); (2) below the existing "Last seen" paragraph in `CardContent`, a "Not yet scanned" or "Last scanned {relative time}" line (reuse the existing `formatRelativeTime` helper) keyed off whether `device.last_scanned_at` is null; (3) a Scan/Re-scan `Button` (`variant="outline"`) below that, with local `$state` tracking an in-flight scanning boolean per card instance — label is `Scan` when `last_scanned_at` is null, `Re-scan` otherwise, becomes `Scanning…` and `disabled` while in-flight; `onclick` calls `onScan(device.id)` (parent owns the actual `triggerScan` API call and post-scan refresh, per the existing `onRegister`/`onMerge` delegation pattern already used in this file) and sets the local scanning flag; wrap the button's disabled-state container in an `aria-live="polite"` region per UI-SPEC's Accessibility Contract so the "Scanning…" transition is announced.

    Create frontend/src/lib/components/ScanResultDialog.svelte: props `{ device, open = $bindable(false) }` where `device` carries `security_status`, `last_scanned_at`, and (for this dialog's display purposes) the most recently known `open_ports`/risky/unexpected breakdown — since Plan 04-02 does not expose a dedicated "get scan detail" route, derive the displayed port list from whatever fields are present on the `device` object passed in by the parent (the dashboard already fetches `/api/devices`; if richer per-port detail is needed beyond `security_status`, the parent may pass an additional prop sourced from the device list response — implement this dialog to gracefully render "Not yet scanned" body copy when no port detail is available, rather than blocking on a new backend route). Use the exact `Dialog`/`DialogContent`/`DialogHeader`/`DialogTitle` import and usage pattern from `RegisterDialog.svelte`. Heading: `Scan Results — {device.name}` per Copywriting Contract. Body: a repeating row list, each row `{port} ({service name})` at Label weight with an inline flag (`Risky — commonly exploited` / `Unexpected for this device type` / `Expected`) colored per the port's classification; footer: `Last scanned {relative time}`. Empty state when no scan has run: heading `Not yet scanned`, body `Run a scan to check this device for open ports and security issues.` per Copywriting Contract.

    Create frontend/src/lib/components/SecurityAlertsBanner.svelte: on `onMount`, call `listAlerts()` from `$lib/api`, store result in `$state<unknown[]>([])`. If the array is empty, render nothing (no DOM output at all, per Copywriting Contract — "no all-clear empty-state banner"). Otherwise render an `Alert` (shadcn-svelte, installed in Task 1) containing: heading `Security Alerts` (per Copywriting Contract, only shown when alerts exist), one row per alert with an alert-type icon (`@lucide/svelte` — e.g. `ShieldAlert` for malicious_ip, `AlertTriangle` for suspicious_traffic/unexpected_port, `Radio` or similar for unknown_device; `aria-hidden="true"` per UI-SPEC Accessibility Contract), the message text per the Copywriting Contract's exact per-type templates (interpolating device name from `alert.device_id`-matched device data where available, falling back to a generic phrasing if no device match exists for a `device_id: null` alert), a relative timestamp (reuse `DeviceCard.svelte`'s `formatRelativeTime` logic — either import it if exported, or replicate the same function locally), and a per-row dismiss `×` button (`aria-label="Dismiss alert"`) that calls `ackAlert(alert.id)` and removes the row from local state immediately, no confirmation. When 2+ alerts are present, also render a banner-level "Dismiss all" (Label weight, `--color-muted`, right-aligned in the banner header) that opens an `AlertDialog` (installed in Task 1) with the exact confirmation copy from the Copywriting Contract (title `Dismiss all alerts?`, body interpolating the current count, confirm button `Dismiss all`, cancel `Cancel`); confirming calls `ackAllAlerts()` and clears local state to `[]`.

    Modify frontend/src/routes/dashboard/+page.svelte: import `SecurityAlertsBanner` from `$lib/components/SecurityAlertsBanner.svelte` and `ScanResultDialog` from `$lib/components/ScanResultDialog.svelte`. Render `<SecurityAlertsBanner />` directly above the existing Phase 2 summary-banner `<div>` (per D-12/UI-SPEC Layout's stacking order: alerts banner, then summary banner, then device grid — insert it as the first element inside `<main>` after the `<h1>`/`<LiveTrafficFeed />` block, before the existing summary-banner div). Add local state for a selected device's scan-dialog (`selectedScanDeviceId`, `scanDialogOpen`, mirroring the existing `selectedIdentityId`/`registerDialogOpen` pattern). Add a `handleScan(deviceId: number)` function that calls `triggerScan(deviceId)` from `$lib/api`, and on success starts a short poll (reuse the existing `loadDevices()` function on an interval, e.g. every 5s for up to 60s, stopping early once the target device's `last_scanned_at` changes) so the result "becomes visible without requiring a manual page refresh" per UI-SPEC's Interaction States contract; pass `onScan={handleScan}` to every `<DeviceCard>` render call in the device grid.
  </action>
  <verify>
    <automated>cd frontend && npx svelte-check --tsconfig ./tsconfig.json 2>&1 | tail -40</automated>
  </verify>
  <done>All 9 behavior cases are implemented and observable in the rendered markup/component logic; svelte-check reports no new errors; DeviceCard.svelte's badge is never color-only (text label always present per Accessibility Contract); SecurityAlertsBanner renders no DOM when alerts are empty.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts This Phase Produces

New symbols introduced by this plan (excluded from drift/source-grounding checks):

- `frontend/src/lib/components/ScanResultDialog.svelte`: new component (props `device`, `open`)
- `frontend/src/lib/components/SecurityAlertsBanner.svelte`: new component
- `frontend/src/lib/api.ts`: `triggerScan(deviceId)`, `listAlerts()`, `ackAlert(alertId)`, `ackAllAlerts()`
- `frontend/src/lib/styles/theme.css`: `--color-good` CSS token
- `frontend/src/lib/components/DeviceCard.svelte`: new `onScan` prop, security badge, Scan/Re-scan button (in-place extension)
- `frontend/src/routes/dashboard/+page.svelte`: `handleScan(deviceId)`, scan-result dialog state, `<SecurityAlertsBanner />` placement (in-place extension)
- `frontend/src/lib/components/ui/tooltip/`, `ui/alert/`, `ui/alert-dialog/`: new shadcn-svelte component installs
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser → /api/security/*, /api/devices | Existing authenticated session-cookie boundary, unchanged by this plan — no new auth surface introduced on the frontend |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|--------------|------------------|
| T-04-11 | Information Disclosure | Alert message text interpolating device names client-side | accept | Device names are already fully visible elsewhere on the same authenticated dashboard (DeviceCard); no new disclosure surface |
| T-04-12 | Tampering | Official shadcn-svelte registry component source (tooltip/alert/alert-dialog) | accept | First-party shadcn-svelte components are exempt from the third-party vetting gate per 04-UI-SPEC.md's Registry Safety section — not a third-party registry |
</threat_model>

<verification>
`cd frontend && npx svelte-check --tsconfig ./tsconfig.json` reports no new type errors. Manually confirm (via the checkpoint in 04-03 being independent of this plan, since 04-04 has no backend-route dependency beyond what 04-02 already shipped) that `DeviceCard.svelte`'s badge text is always rendered alongside color (grep the component for three literal Badge-label strings: "Good", "Warning", "Critical").
</verification>

<success_criteria>
- Every registered `DeviceCard` shows a colored + text-labeled security badge and a Scan/Re-scan button.
- `ScanResultDialog` and `SecurityAlertsBanner` exist and match the UI-SPEC's Copywriting Contract.
- Dashboard renders the alerts banner above the existing summary banner, hidden entirely when no unacknowledged alerts exist.
- `--color-good` token exists and is used exclusively for the "Good" badge state.
- `svelte-check` passes with no new errors.
</success_criteria>

<output>
Create `.planning/phases/04-security/04-04-SUMMARY.md` when done
</output>
