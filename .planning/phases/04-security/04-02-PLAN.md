---
phase: 04-security
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - backend/src/routes/security.py
  - backend/src/routes/capture.py
  - backend/src/routes/devices.py
  - backend/src/services/discovery.py
  - backend/src/services/bandwidth_anomaly.py
  - backend/src/main.py
  - backend/tests/test_security_scan.py
  - backend/tests/test_security_alerts.py
  - backend/tests/test_bandwidth_anomaly.py
  - backend/tests/test_capture.py
  - backend/tests/test_devices.py
autonomous: true
requirements: [SEC-01, SEC-02, SEC-03, SEC-04]

must_haves:
  truths:
    - "User can trigger an on-demand scan for a device via POST /api/security/scan/{device_id}, which queues a pending_scan_requests row"
    - "Capture container's scan result POST recomputes and persists the device's security_status, evaluating risky/unexpected ports per D-05/D-06"
    - "An unregistered device joining the network writes a security_alerts row of type unknown_device exactly once per newly-discovered identity"
    - "A device's traffic ingest is checked against the static malicious-IP blocklist; a hit writes a security_alerts row and flips security_status to critical"
    - "GET /api/devices includes security_status and last_scanned_at per device"
    - "GET /api/security/alerts lists unacknowledged alerts; POST /api/security/alerts/{id}/ack marks one acknowledged"
    - "User can fetch a device's latest scan-result detail (open ports + risky/unexpected/expected classification) via GET /api/security/scan/{device_id}"
    - "Registered devices' current IP is tracked on Device.last_known_ip so the capture container has a scan target, sourced only from ARP observations"
    - "The capture container can ask the backend to queue a daily re-scan for every registered device without needing an authenticated session"
    - "Every call to POST /api/capture/queue-daily-scans evaluates check_bandwidth_anomaly() for each registered device and, on a True result, writes a security_alerts row of type suspicious_traffic and recomputes/persists that device's security_status (D-09's bandwidth-anomaly signal is reachable in production, not just unit-tested in isolation)"
  artifacts:
    - path: "backend/src/routes/security.py"
      provides: "POST /scan/{device_id}, GET /scan/{device_id}, GET /alerts, POST /alerts/{id}/ack, POST /alerts/ack-all routes, all auth-gated"
      exports: ["router"]
    - path: "backend/src/services/bandwidth_anomaly.py"
      provides: "check_bandwidth_anomaly() read-only query against bandwidth_metrics with a minimum-sample-count guard"
      exports: ["check_bandwidth_anomaly"]
  key_links:
    - from: "backend/src/routes/capture.py"
      to: "backend/src/services/port_rules.py"
      via: "evaluate_open_ports() call inside the new /scan ingest route"
      pattern: "evaluate_open_ports\\("
    - from: "backend/src/routes/capture.py"
      to: "backend/src/services/security_status.py"
      via: "derive_status() call to recompute and persist Device.security_status"
      pattern: "derive_status\\("
    - from: "backend/src/services/discovery.py"
      to: "backend/src/models/security_alert.py"
      via: "SecurityAlert(type=SecurityAlertType.UNKNOWN_DEVICE, ...) insert on new-identity creation"
      pattern: "SecurityAlertType.UNKNOWN_DEVICE"
    - from: "backend/src/routes/capture.py"
      to: "backend/src/services/threat_intel_source.py"
      via: "get_default_threat_intel_source().is_malicious(dst_ip) check on traffic ingest"
      pattern: "is_malicious\\("
    - from: "backend/src/routes/devices.py"
      to: "backend/src/models/device.py"
      via: "_serialize_device() includes security_status/last_scanned_at fields"
      pattern: "security_status"
    - from: "backend/src/routes/security.py"
      to: "backend/src/models/port_scan_result.py"
      via: "GET /scan/{device_id} reads the latest PortScanResult row and re-runs evaluate_open_ports() for display"
      pattern: "GET.*scan.*device_id|PortScanResult"
    - from: "backend/src/routes/capture.py"
      to: "backend/src/models/device.py"
      via: "ingest_arp persists Device.last_known_ip for the matching registered device"
      pattern: "last_known_ip"
    - from: "backend/src/routes/capture.py"
      to: "backend/src/services/bandwidth_anomaly.py"
      via: "queue-daily-scans calls check_bandwidth_anomaly() per registered device and inserts a SUSPICIOUS_TRAFFIC SecurityAlert + derive_status() recompute on a True result"
      pattern: "check_bandwidth_anomaly\\("
---

<objective>
Wire Phase 4's backend request/response and event-driven surfaces on top of Plan 04-01's schema and pure-function services: a new `/api/security/*` route file (scan-trigger, alerts list/ack), extensions to the existing `/api/capture/*` ingest routes (scan-result ingest, pending-scan poll target, a daily-rescan queue trigger that also runs the bandwidth-anomaly check, ARP-sourced IP tracking, and a malicious-IP check on every traffic ingest), the SEC-02 unknown-device alert hook in `discovery.py` (D-13: SEC-02/SEC-04 both write into `security_alerts` for visibility now, delivery deferred to Phase 5), a new read-only bandwidth-anomaly query service (D-09) wired into the daily-rescan queue trigger so its signal actually reaches `security_alerts`/`Device.security_status`, and the `devices.py` serializer extension. This plan does not touch the capture container's own code (that's Plan 04-03) or the frontend (Plan 04-04) — it completes the full backend API surface those later plans depend on.

Purpose: Gives the capture container (04-03) and frontend (04-04) a stable, fully-tested API contract to build against, and closes SEC-02/SEC-03's detection-and-recording requirements end-to-end on the backend side — including the bandwidth-anomaly half of SEC-03, which must be reachable from a real execution path, not just covered by an isolated unit test.
Output: 1 new route file, 1 new service, 3 modified route/service files, `main.py` router registration, 5 new/extended pytest files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-security/04-CONTEXT.md
@.planning/phases/04-security/04-RESEARCH.md
@.planning/phases/04-security/04-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: security.py route + bandwidth_anomaly.py service</name>
  <files>backend/src/routes/security.py, backend/src/services/bandwidth_anomaly.py, backend/src/main.py, backend/tests/test_security_scan.py, backend/tests/test_bandwidth_anomaly.py</files>
  <read_first>
    backend/src/routes/traffic.py (full file — auth-gated GET route shape, _resolve_device_macs() helper to reuse directly via import, not duplicate)
    backend/src/routes/devices.py (full file — 404-on-missing pattern, db.add()+db.commit() insert pattern)
    backend/src/auth.py (require_auth dependency signature)
    backend/src/main.py (current router-registration block — app.include_router(...) calls)
    backend/src/services/security_status.py (derive_status signature from Plan 04-01)
    backend/src/models/security_alert.py (SecurityAlert/SecurityAlertType shape from Plan 04-01)
    backend/src/models/pending_scan_request.py (PendingScanRequest shape from Plan 04-01)
  </read_first>
  <behavior>
    - Test 1 (test_security_scan.py): authenticated POST /api/security/scan/{device_id} for an existing registered device returns 202 and creates exactly one PendingScanRequest row with claimed_at=None
    - Test 2 (test_security_scan.py): authenticated POST /api/security/scan/{device_id} for a nonexistent device_id returns 404
    - Test 3 (test_security_scan.py): unauthenticated POST /api/security/scan/{device_id} returns 401
    - Test 4 (test_security_scan.py): authenticated GET /api/security/alerts returns only unacknowledged SecurityAlert rows (seed one acknowledged=True and one acknowledged=False row directly via the test_db session; response contains only the unacknowledged one)
    - Test 5 (test_security_scan.py): authenticated POST /api/security/alerts/{id}/ack on an existing alert sets acknowledged=True and returns 200; a subsequent GET /api/security/alerts no longer includes it
    - Test 6 (test_security_scan.py): authenticated POST /api/security/alerts/{id}/ack on a nonexistent alert id returns 404
    - Test 7 (test_security_scan.py): authenticated POST /api/security/alerts/ack-all marks every currently-unacknowledged alert acknowledged=True in one call
    - Test 8 (test_security_scan.py): authenticated GET /api/security/scan/{device_id} for a device with at least one PortScanResult row returns the latest scan's open_ports list, each port annotated with a "flag" of "risky"/"unexpected"/"expected" per evaluate_open_ports(), plus scanned_at — for a ROUTER-type device with open_ports=[22, 23], port 22 is flagged "expected", port 23 is flagged "risky"
    - Test 9 (test_security_scan.py): authenticated GET /api/security/scan/{device_id} for a device with zero PortScanResult rows returns {"scanned_at": None, "ports": []} (never a 404 — "not yet scanned" is a valid, displayable state, not an error)
    - Test 10 (test_security_scan.py): authenticated GET /api/security/scan/{device_id} for a nonexistent device_id returns 404
    - Test 11 (test_security_scan.py): unauthenticated GET /api/security/scan/{device_id} returns 401
    - Test 12 (test_bandwidth_anomaly.py): check_bandwidth_anomaly() against a device with fewer than 7 distinct prior days of BandwidthMetric rows returns False (RESEARCH.md Pitfall 5's minimum-sample-count guard) regardless of how large the most recent reading is
    - Test 13 (test_bandwidth_anomaly.py): check_bandwidth_anomaly() against a device with >=7 distinct prior days of low/steady BandwidthMetric rows, then one new rollup at 3x+ the rolling average, returns True
    - Test 14 (test_bandwidth_anomaly.py): check_bandwidth_anomaly() against a device with >=7 distinct prior days at steady volume and a new rollup within the same steady range returns False
  </behavior>
  <action>
    Create backend/src/routes/security.py with `router = APIRouter()`. Import `require_auth` from `src.auth`, `get_db` from `src.database`, `Device` from `src.models.device`, `PendingScanRequest` from `src.models.pending_scan_request`, `SecurityAlert, SecurityAlertType` from `src.models.security_alert`, `PortScanResult` from `src.models.port_scan_result`, `evaluate_open_ports` from `src.services.port_rules`.
    - `POST /scan/{device_id}` (`status_code=202`): 404 if `Device` lookup by id misses (mirror traffic.py's `_resolve_device_macs` 404 shape); else `db.add(PendingScanRequest(device_id=device_id))`, `await db.commit()`, return `{"ok": True, "device_id": device_id}`. Gated with `Depends(require_auth)`.
    - `GET /scan/{device_id}`: 404 if `Device` lookup by id misses; else `select(PortScanResult).where(PortScanResult.device_id == device_id).order_by(PortScanResult.scanned_at.desc()).limit(1)` to fetch the latest scan row. If no row exists, return `{"scanned_at": None, "ports": []}`. If a row exists, call `evaluate_open_ports(device.type, result.open_ports)` to get `(risky_open, unexpected_open)`; build `ports: list[dict]` — one entry per port in `result.open_ports`, each `{"port": p, "flag": "risky" if p in risky_open else "unexpected" if p in unexpected_open else "expected"}`; return `{"scanned_at": result.scanned_at, "ports": ports}`. This route never triggers a scan itself (read-only) and never duplicates `evaluate_open_ports`' classification logic — it calls the same Plan 04-01 pure function the capture-ingest route (`POST /api/capture/scan`) already calls, so the displayed flags are always consistent with whatever last set `Device.security_status`.
    - `GET /alerts`: `select(SecurityAlert).where(SecurityAlert.acknowledged == False).order_by(SecurityAlert.created_at.desc())`, returns a list of plain dicts (id, device_id, type, severity, message, created_at, acknowledged).
    - `POST /alerts/{alert_id}/ack`: 404 if `SecurityAlert` lookup by id misses; else set `alert.acknowledged = True`, `await db.commit()`, return the updated dict.
    - `POST /alerts/ack-all`: bulk acknowledge — select all unacknowledged alerts, set each `.acknowledged = True`, one `await db.commit()`, return `{"acknowledged_count": N}`.
    Register in backend/src/main.py: `from src.routes import auth, capture, devices, security, traffic` (add `security` to the existing import line) and `app.include_router(security.router, prefix="/api/security")` alongside the existing `include_router` calls.

    Create backend/src/services/bandwidth_anomaly.py. Import `_resolve_device_macs` from `src.routes.traffic` (reuse directly, do not duplicate — per 04-PATTERNS.md's explicit instruction). Define module-level named constants (not magic numbers, per D-09's discretion and RESEARCH.md Open Question 2): `ROLLING_WINDOW_DAYS = 14`, `ANOMALY_THRESHOLD_MULTIPLIER = 3.0`, `MIN_SAMPLE_DAYS = 7`. Function `async def check_bandwidth_anomaly(db: AsyncSession, device_id: int) -> bool`: resolve the device's MAC set via `_resolve_device_macs`; query `BandwidthMetric` rows for those MACs within the last `ROLLING_WINDOW_DAYS` days; group by calendar day (Python-side grouping by `row.time.date()`, not SQL `time_bucket` — keep this portable across Postgres/SQLite per the existing `network_bandwidth` route's dual-path precedent); if fewer than `MIN_SAMPLE_DAYS` distinct days have data, return False (Pitfall 5 guard); otherwise compute the rolling average bytes-per-day across all days except the most recent, compare the most recent day's total against `rolling_average * ANOMALY_THRESHOLD_MULTIPLIER`, return True if it exceeds that threshold else False. Sum `bytes_rx + bytes_tx` per day as the "traffic volume" figure. This function is a pure read+compute query with no side effects (no alert insert, no status write) — Task 2's `queue-daily-scans` route is the only caller that acts on its result.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_security_scan.py tests/test_bandwidth_anomaly.py -x -v</automated>
  </verify>
  <done>All 14 behavior cases pass; security.py router is registered under /api/security in main.py; every security.py route (including the new GET /scan/{device_id}) is gated behind Depends(require_auth); GET /scan/{device_id} reuses evaluate_open_ports() rather than re-implementing port classification.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: capture.py extensions (scan ingest, pending-scan poll, daily-rescan queue trigger + bandwidth-anomaly evaluation, ARP-sourced IP tracking, malicious-IP check on traffic ingest) + devices.py serializer + discovery.py SEC-02 alert hook</name>
  <files>backend/src/routes/capture.py, backend/src/routes/devices.py, backend/src/services/discovery.py, backend/tests/test_capture.py, backend/tests/test_security_alerts.py, backend/tests/test_devices.py</files>
  <read_first>
    backend/src/routes/capture.py (full current file — the exact _TRUSTED_HOSTS check, Pydantic payload shapes, _MAX_FLOWS_PER_ROLLUP bounding precedent; modify in place, do not introduce a second trust-boundary implementation)
    backend/src/services/discovery.py (full current file — upsert_discovered_identity()'s pg_insert/sqlite_insert dialect branching; record_observation()'s Device-branch vs new-identity branch)
    backend/src/routes/devices.py (full current file — _serialize_device() dict shape, modify in place)
    backend/src/services/port_rules.py, security_status.py, threat_intel_source.py, bandwidth_anomaly.py (from Plan 04-01 and Task 1 of this plan — exact function signatures to call)
    backend/src/models/port_scan_result.py, pending_scan_request.py, security_alert.py, device.py (from Plan 04-01 — exact column names, including the new last_known_ip column)
  </read_first>
  <behavior>
    - Test 1 (test_capture.py): non-loopback POST to /api/capture/scan returns 403 (reuses the existing ASGITransport client.host override technique already used for /arp,/dhcp,/mdns,/traffic trust-boundary tests in this file)
    - Test 2 (test_capture.py): loopback POST /api/capture/scan with {"device_id": <id>, "open_ports": [22, 23]} for a ROUTER-type device creates a PortScanResult row, sets Device.last_scanned_at to now, and updates Device.security_status to "critical" (port 23 is risky)
    - Test 3 (test_capture.py): loopback POST /api/capture/scan with open_ports exceeding a max length (e.g. 1001 entries) returns 413, mirroring the existing _MAX_FLOWS_PER_ROLLUP precedent
    - Test 4 (test_capture.py): loopback GET /api/capture/pending-scans returns {"pending": [{"device_id": ..., "ip": ...}]} for every PendingScanRequest row with claimed_at=None whose Device has a non-null last_known_ip, marks each returned row's claimed_at as now (claim-on-read, preventing duplicate delivery to a second poll before the result is POSTed back); a PendingScanRequest whose Device has last_known_ip=None is left unclaimed (not returned, claimed_at stays None) so it can be retried once an IP becomes known
    - Test 5 (test_capture.py): loopback POST /api/capture/queue-daily-scans creates exactly one PendingScanRequest per registered Device row (querying the devices table, never discovered_identities/arp_events/etc — RESEARCH.md Pitfall 2), and does NOT create a duplicate PendingScanRequest for a device that already has one unclaimed
    - Test 6 (test_capture.py): non-loopback POST to /api/capture/queue-daily-scans returns 403
    - Test 7 (test_capture.py): loopback POST /api/capture/arp with a src_mac matching an existing registered Device's last_known_mac updates that Device's last_known_ip to the payload's src_ip
    - Test 8 (test_capture.py): loopback POST /api/capture/traffic with a flow whose dst_ip matches a known-malicious test fixture IP creates exactly one SecurityAlert row with type=malicious_ip and flips the matching Device's security_status to "critical" (inject a test-only ThreatIntelSource fixture via dependency override or monkeypatch — do not require the real vendored blocklist file for this test's determinism)
    - Test 9 (test_security_alerts.py): record_observation() creating a brand-new DiscoveredIdentity row (first time this identity_key is seen) also creates exactly one SecurityAlert row with type=unknown_device, device_id=None
    - Test 10 (test_security_alerts.py): record_observation() called a second time for the SAME identity_key (update path, not insert) does NOT create a second SecurityAlert row — only the very first sighting of a new identity fires the alert
    - Test 11 (test_devices.py, extend existing file): GET /api/devices response includes "security_status" and "last_scanned_at" keys for every registered device in the existing test fixtures
    - Test 12 (test_capture.py): loopback POST /api/capture/queue-daily-scans, for a registered device whose BandwidthMetric history makes check_bandwidth_anomaly() return True (seed >=7 distinct prior days of steady-volume rows plus one most-recent rollup at 3x+ that average, directly via the test_db session), creates exactly one SecurityAlert row with type=suspicious_traffic for that device AND flips that device's security_status to "warning" via derive_status() (assert both the alert row and the persisted Device.security_status — not just one or the other)
    - Test 13 (test_capture.py): loopback POST /api/capture/queue-daily-scans, for a registered device whose BandwidthMetric history makes check_bandwidth_anomaly() return False (fewer than 7 distinct days, or steady volume with no spike), creates no suspicious_traffic SecurityAlert for that device and does not flip its security_status to "warning" on this basis alone
    - Test 14 (test_capture.py): loopback POST /api/capture/queue-daily-scans called twice in a row for the same device with a sustained anomalous bandwidth pattern (check_bandwidth_anomaly() still returns True on the second call) does NOT create a second suspicious_traffic SecurityAlert if an unacknowledged one already exists for that device — mirrors the existing malicious-IP/unexpected-port dedupe pattern, preventing alert-spam on every daily cadence
  </behavior>
  <action>
    Modify backend/src/routes/capture.py:
    - Add `ScanResultPayload(BaseModel)` with fields `device_id: int`, `open_ports: list[int]`. Add module constant `_MAX_OPEN_PORTS = 1000` near the existing `_MAX_FLOWS_PER_ROLLUP`.
    - Add `POST /scan` (status_code=201): same `_TRUSTED_HOSTS` check as every existing route in this file (copy verbatim, do not reimplement); if `len(payload.open_ports) > _MAX_OPEN_PORTS` raise 413; load the `Device` by `payload.device_id` (404 if missing); insert `PortScanResult(device_id=device.id, open_ports=payload.open_ports)`; call `evaluate_open_ports(device.type, payload.open_ports)` from `src.services.port_rules` to get `(risky_open, unexpected_open)`. Determine `has_malicious_ip_match`/`has_bandwidth_anomaly` by checking for any existing unacknowledged `SecurityAlert` row for this `device_id` with `type == SecurityAlertType.MALICIOUS_IP` / `type == SecurityAlertType.SUSPICIOUS_TRAFFIC` respectively (so a prior critical/warning signal is not silently cleared by a clean scan result — D-07's "recomputed whenever new scan results land or a new traffic-pattern/malicious-IP match is recorded" means each signal source independently contributes, not that a scan resets everything). Call `derive_status(risky_open_ports=risky_open, unexpected_open_ports=unexpected_open, has_malicious_ip_match=..., has_bandwidth_anomaly=...)` from `src.services.security_status`; set `device.security_status = result.value`, `device.last_scanned_at = now`. If `unexpected_open` is non-empty, also insert `SecurityAlert(device_id=device.id, type=SecurityAlertType.UNEXPECTED_PORT, severity=SecurityStatus.WARNING, message=f"{device.name} has an unexpected open port")`. Commit once at the end.
    - Add `GET /pending-scans`: same `_TRUSTED_HOSTS` check; select `PendingScanRequest` joined to `Device` where `claimed_at IS NULL` and `Device.last_known_ip IS NOT NULL`; for each matched row, set `claimed_at = now`; commit; return `{"pending": [{"device_id": r.device_id, "ip": device.last_known_ip} for each]}`. Rows whose Device has no `last_known_ip` yet are simply excluded from this response (left unclaimed, retried on a later poll once an ARP observation populates the IP).
    - Add `POST /queue-daily-scans` (status_code=201): same `_TRUSTED_HOSTS` check; import `check_bandwidth_anomaly` from `src.services.bandwidth_anomaly` (Task 1 of this plan) at the top of the file alongside the other service imports; query `select(Device)` (the registered-devices table directly, never `discovered_identities`/`arp_events`/etc — RESEARCH.md Pitfall 2); for each `Device`: (1) check whether an unclaimed `PendingScanRequest` already exists for that `device_id` (skip the queue insert if so, to avoid duplicate queueing on repeated daily-loop calls or restarts); otherwise insert a new `PendingScanRequest(device_id=device.id)`; (2) independently of the queue-insert step, call `await check_bandwidth_anomaly(db, device.id)` — on `True`, check for an existing unacknowledged `SecurityAlert` row for this `device_id` with `type == SecurityAlertType.SUSPICIOUS_TRAFFIC`; if none exists, insert `SecurityAlert(device_id=device.id, type=SecurityAlertType.SUSPICIOUS_TRAFFIC, severity=SecurityStatus.WARNING, message=f"{device.name} shows unusually high traffic volume")` and recompute `device.security_status` by calling `derive_status()` with `has_bandwidth_anomaly=True` and the device's currently-known `has_malicious_ip_match`/risky-and-unexpected-port state (derive the latter two the same way `POST /scan` does: check for existing unacknowledged MALICIOUS_IP alert, and re-derive risky/unexpected ports from the device's latest `PortScanResult` via `evaluate_open_ports` if one exists, else empty lists), persisting the result onto `device.security_status`. If an unacknowledged SUSPICIOUS_TRAFFIC alert already exists for the device, skip the insert (Test 14's dedupe) but the status recompute is unnecessary in that case since the prior alert already drove it to at least warning. Commit once at the end (queue inserts and anomaly-driven alert/status writes share the same transaction). Return `{"queued_count": N}`.
    - Modify `ingest_arp`: after the existing `ArpEvent` insert and `record_observation` call, additionally look up `select(Device).where(Device.last_known_mac == payload.src_mac)`; if found, set `device.last_known_ip = payload.src_ip` and commit (separately from the ArpEvent/record_observation commits, or folded into one commit — either is acceptable as long as the column is persisted). This is the only write path for `Device.last_known_ip` in this plan; `record_observation`'s own Device-branch is not modified here since `ingest_arp` already has direct access to `src_ip` and `record_observation`'s `Observation` dataclass has no IP field to thread through.
    - Modify `ingest_traffic`: after the existing per-flow `TrafficFlow` insert loop and `BandwidthMetric` write, for each distinct `flow.dst_ip` in the rollup, call `get_default_threat_intel_source().is_malicious(flow.dst_ip)`; on a hit, resolve the `Device` via `flow.src_mac` (look up `Device` by `last_known_mac == flow.src_mac`; if no matching Device, skip the alert — only registered devices get malicious-IP alerts/status flips per this phase's scope), insert `SecurityAlert(device_id=device.id, type=SecurityAlertType.MALICIOUS_IP, severity=SecurityStatus.CRITICAL, message=f"{device.name} contacted a known-malicious address")`, and set `device.security_status = SecurityStatus.CRITICAL.value`. Guard against inserting a duplicate alert for the same device+dst_ip within the same ingest call (dedupe within the loop via a local `seen_hits: set[tuple[int, str]]`, not across calls).

    Modify backend/src/routes/devices.py: in `_serialize_device()`, add `"security_status": device.security_status` and `"last_scanned_at": device.last_scanned_at` as new dict keys, inserted in the same flat style as every existing key.

    Modify backend/src/services/discovery.py: in `upsert_discovered_identity()`, run a `select(DiscoveredIdentity.id).where(DiscoveredIdentity.identity_key == identity_key)` pre-check before executing the dialect-aware upsert statement; if it returns no row, this is a brand-new identity — after the upsert commits, insert `SecurityAlert(device_id=None, type=SecurityAlertType.UNKNOWN_DEVICE, severity=SecurityStatus.WARNING, message="Unknown device joined the network")` and commit again. If the pre-check finds an existing row, skip the alert insert entirely (update path, per Test 10). Import `SecurityAlert, SecurityAlertType` from `src.models.security_alert` and `SecurityStatus` from `src.services.security_status` at the top of discovery.py.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_capture.py tests/test_security_alerts.py tests/test_devices.py -x -v</automated>
  </verify>
  <done>All 14 behavior cases pass; /api/capture/scan, /api/capture/pending-scans, and /api/capture/queue-daily-scans all reuse the file's existing _TRUSTED_HOSTS check verbatim; ingest_traffic's malicious-IP check never crashes the route on a ThreatIntelSource lookup failure; record_observation fires exactly one unknown_device alert per genuinely-new identity, never on updates to an existing identity; ingest_arp persists last_known_ip onto a matching registered Device; queue-daily-scans calls check_bandwidth_anomaly() for every registered device and, on a True result with no existing unacknowledged suspicious_traffic alert, writes the alert and persists an updated Device.security_status via derive_status() — D-09's bandwidth-anomaly signal is reachable from a real execution path, not only from its own isolated unit test.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts This Phase Produces

New symbols introduced by this plan (excluded from drift/source-grounding checks):

- `backend/src/routes/security.py`: `router` — `POST /scan/{device_id}`, `GET /scan/{device_id}`, `GET /alerts`, `POST /alerts/{alert_id}/ack`, `POST /alerts/ack-all`
- `backend/src/services/bandwidth_anomaly.py`: `ROLLING_WINDOW_DAYS`, `ANOMALY_THRESHOLD_MULTIPLIER`, `MIN_SAMPLE_DAYS`, `check_bandwidth_anomaly(db, device_id)`
- `backend/src/routes/capture.py`: `ScanResultPayload`, `_MAX_OPEN_PORTS`, `POST /scan`, `GET /pending-scans`, `POST /queue-daily-scans` (new routes alongside the existing `/arp`,`/dhcp`,`/mdns`,`/traffic`; `queue-daily-scans` also calls `check_bandwidth_anomaly()` per device and writes `SUSPICIOUS_TRAFFIC` alerts/status updates on a hit)
- `backend/src/routes/devices.py`: `_serialize_device()` gains `security_status`, `last_scanned_at` dict keys (in-place extension, not a new symbol)
- `backend/src/services/discovery.py`: `upsert_discovered_identity()` gains the new-identity `SecurityAlert` insert (in-place extension)
- `backend/src/main.py`: `security` router registration under `/api/security`
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser → /api/security/* | Authenticated session-cookie boundary, same as every existing route (no new auth surface) |
| Capture container → /api/capture/scan, /api/capture/pending-scans, /api/capture/queue-daily-scans | Loopback/gateway-trusted-only, identical to existing /arp,/dhcp,/mdns,/traffic boundary |
| Traffic ingest → ThreatIntelSource | Internal read-only lookup against a build-time-bundled file, no external call |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|--------------|------------------|
| T-04-04 | Spoofing / Tampering | Forged POST to /api/capture/scan, /api/capture/queue-daily-scans from a non-capture source | mitigate | Reuses the existing `_TRUSTED_HOSTS` loopback/gateway check verbatim — no second trust-boundary implementation introduced |
| T-04-05 | Denial of Service | Oversized `open_ports` payload to /api/capture/scan | mitigate | `_MAX_OPEN_PORTS = 1000` bound, mirrors the existing `_MAX_FLOWS_PER_ROLLUP` precedent, returns 413 |
| T-04-06 | Tampering | A malicious/compromised capture process claims pending scans without ever completing them, or repeatedly calls queue-daily-scans | accept | Capture container is already a fully-trusted privileged component per D-03/Phase 1's threat model — this is not a new trust boundary, just new message types across an already-trusted channel; queue-daily-scans is explicitly idempotent (skips devices with an existing unclaimed request, and skips a duplicate suspicious_traffic alert if one is already unacknowledged) so repeated calls cannot flood the queue or alert feed |
| T-04-07 | Repudiation | Unknown-device alert could fire repeatedly for the same identity on every observation if the insert-vs-update distinction is implemented incorrectly | mitigate | Explicit pre-check `select(...).where(identity_key == ...)` before the upsert distinguishes insert from update; Test 10 directly verifies no duplicate alert on a second observation of the same identity |
| T-04-08 | Business Logic | Scan triggered for a device_id that does not belong to the requesting household (not applicable here — single-user/household, no multi-tenant boundary) | accept | No multi-tenant concern exists in this product (PROJECT.md: single shared password, no RBAC) |
| T-04-09 | Information Disclosure / Repudiation | Bandwidth-anomaly false positives (e.g. a large download or video call) sustained across multiple daily-scan cadences spam the alert feed and obscure genuine signals | accept | D-09 explicitly scopes this as a `warning`-level, false-positive-prone signal; Test 14's existing-unacknowledged-alert dedupe caps it to one open alert per device until the user acknowledges it, rather than re-alerting every cadence |
</threat_model>

<verification>
`cd backend && python -m pytest` (full suite) confirms no regression in existing capture/devices/discovery test files alongside the new behavior. Manually trace one POST /api/capture/scan request through evaluate_open_ports -> derive_status -> Device.security_status persistence to confirm no lazy-read-time computation was introduced (RESEARCH.md Anti-Pattern: "Computing security_status lazily at /api/devices read time"). Also trace one POST /api/capture/queue-daily-scans call through check_bandwidth_anomaly -> SecurityAlert insert -> derive_status -> Device.security_status persistence to confirm the bandwidth-anomaly signal reaches the same persisted-status path as the other three signals, not a dead-end unit test.
</verification>

<success_criteria>
- `POST /api/security/scan/{device_id}`, `GET /api/security/scan/{device_id}`, `/api/security/alerts`, `/api/security/alerts/{id}/ack`, `/api/security/alerts/ack-all` all exist, auth-gated, and pass their behavior tests.
- `GET /api/security/scan/{device_id}` returns the latest scan's per-port risky/unexpected/expected classification (or an empty not-yet-scanned shape), reusing `evaluate_open_ports()` rather than re-deriving classification logic.
- `/api/capture/scan`, `/api/capture/pending-scans`, `/api/capture/queue-daily-scans` all exist, loopback/gateway-trusted, and pass their behavior tests.
- `ingest_arp` persists `Device.last_known_ip` for any registered device matched by MAC.
- `ingest_traffic` checks every distinct `dst_ip` against the default `ThreatIntelSource` and writes a `malicious_ip` alert + critical status flip on a hit.
- `record_observation` writes exactly one `unknown_device` alert per genuinely-new `DiscoveredIdentity`, never on updates.
- `GET /api/devices` responses include `security_status` and `last_scanned_at`.
- `POST /api/capture/queue-daily-scans` calls `check_bandwidth_anomaly()` for every registered device and, on a `True` result with no existing unacknowledged `suspicious_traffic` alert, writes a `SecurityAlert(type=SUSPICIOUS_TRAFFIC)` row and persists an updated `Device.security_status` via `derive_status()` — the bandwidth-anomaly half of SEC-03/D-09 is reachable end-to-end, not dead code behind an isolated unit test.
- Full backend test suite passes with no regressions.
</success_criteria>

<output>
Create `.planning/phases/04-security/04-02-SUMMARY.md` when done
</output>
</content>
