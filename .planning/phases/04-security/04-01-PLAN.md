---
phase: 04-security
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/models/device.py
  - backend/src/models/port_scan_result.py
  - backend/src/models/security_alert.py
  - backend/src/models/pending_scan_request.py
  - backend/src/services/port_rules.py
  - backend/src/services/security_status.py
  - backend/src/services/threat_intel_source.py
  - backend/src/data/firehol_level1.netset
  - backend/alembic/versions/0005_security.py
  - backend/tests/conftest.py
  - backend/tests/test_port_rules.py
  - backend/tests/test_security_status.py
  - backend/tests/test_threat_intel.py
autonomous: true
requirements: [SEC-01, SEC-03, SEC-04]

must_haves:
  truths:
    - "A device's open ports can be classified into risky vs unexpected-for-its-type buckets via a pure function"
    - "A device's good/warning/critical status can be derived from scan + threat + anomaly signals via a pure function, defaulting to good when never scanned"
    - "A malicious-IP lookup works against a vendored static blocklist with zero runtime network calls"
    - "Device (incl. new last_known_ip column), port_scan_results, security_alerts, and pending_scan_requests tables exist with the correct shape after migration"
  artifacts:
    - path: "backend/src/services/port_rules.py"
      provides: "RISKY_PORTS frozenset, EXPECTED_PORTS dict keyed by DeviceType, evaluate_open_ports() pure function"
      exports: ["RISKY_PORTS", "EXPECTED_PORTS", "evaluate_open_ports"]
    - path: "backend/src/services/security_status.py"
      provides: "SecurityStatus enum and derive_status() pure function"
      exports: ["SecurityStatus", "derive_status"]
    - path: "backend/src/services/threat_intel_source.py"
      provides: "ThreatIntelSource Protocol and StaticBlocklistSource implementation"
      exports: ["ThreatIntelSource", "StaticBlocklistSource", "get_default_threat_intel_source"]
    - path: "backend/src/models/security_alert.py"
      provides: "SecurityAlert model (security_alerts table)"
      contains: "class SecurityAlert"
    - path: "backend/src/models/port_scan_result.py"
      provides: "PortScanResult model (port_scan_results history table)"
      contains: "class PortScanResult"
    - path: "backend/src/models/pending_scan_request.py"
      provides: "PendingScanRequest model (pending_scan_requests queue table)"
      contains: "class PendingScanRequest"
    - path: "backend/alembic/versions/0005_security.py"
      provides: "Migration creating all four new/modified tables"
      contains: "down_revision = \"0004\""
  key_links:
    - from: "backend/src/services/port_rules.py"
      to: "backend/src/models/device.py"
      via: "DeviceType enum import"
      pattern: "from src.models.device import DeviceType"
    - from: "backend/src/models/device.py"
      to: "security_status column"
      via: "SAEnum(SecurityStatus, ...) mapped_column with server_default"
      pattern: "security_status.*Mapped"
---

<objective>
Build Phase 4's foundational data layer: four new/modified tables (Device gains `security_status`/`last_scanned_at`; new `port_scan_results`, `security_alerts`, `pending_scan_requests`) plus three framework-free pure-function/Protocol services (`port_rules.py`, `security_status.py`, `threat_intel_source.py`) that the routes and capture-container work in later waves will call. This plan ships no routes and no capture-container code — it is the schema + business-logic substrate per D-05/D-06/D-07/D-08/D-11.

Purpose: Establishes the testable, table-driven core (per D-05/D-06: "no opaque scoring formula") that every other Phase 4 plan depends on, and gets the Alembic migration merged first since schema changes are the highest-risk-to-delay item.
Output: 4 SQLAlchemy models (1 modified, 3 new), 3 backend services, 1 Alembic migration, 1 vendored blocklist data file, 3 new pytest files (all pure-function/unit style, fixture-free per RESEARCH.md's Wave 0 Gaps).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-security/04-CONTEXT.md
@.planning/phases/04-security/04-RESEARCH.md
@.planning/phases/04-security/04-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure-function services — port_rules.py, security_status.py, threat_intel_source.py</name>
  <files>backend/src/services/port_rules.py, backend/src/services/security_status.py, backend/src/services/threat_intel_source.py, backend/src/data/firehol_level1.netset, backend/tests/test_port_rules.py, backend/tests/test_security_status.py, backend/tests/test_threat_intel.py</files>
  <read_first>
    backend/src/models/device.py (exact DeviceType enum member names: PHONE/LAPTOP/DESKTOP/TABLET/IOT/TV/CONSOLE/ROUTER/OTHER — do not invent new names)
    backend/src/services/bandwidth_source.py (the exact Protocol + concrete-impl shape ThreatIntelSource/StaticBlocklistSource must mirror, including the docstring convention naming the future swap-in implementation)
    backend/src/services/domain_grouping.py (pure-function, no-DB-import, fixture-free style to replicate in port_rules.py/security_status.py)
  </read_first>
  <behavior>
    - Test 1 (test_port_rules.py): evaluate_open_ports(DeviceType.ROUTER, [22, 53, 23]) returns risky_open=[23], unexpected_open=[] (22/53 are in ROUTER's allowlist, 23 is universally risky)
    - Test 2 (test_port_rules.py): evaluate_open_ports(DeviceType.PHONE, [32400]) returns risky_open=[], unexpected_open=[32400] (not risky, not in PHONE's empty allowlist)
    - Test 3 (test_port_rules.py): evaluate_open_ports(DeviceType.IOT, []) returns ([], []) — no open ports means no flags
    - Test 4 (test_port_rules.py): evaluate_open_ports(DeviceType.OTHER, [3389, 80]) returns risky_open=[3389], unexpected_open=[80] (3389/RDP universally risky; 80 not in OTHER's empty allowlist)
    - Test 5 (test_security_status.py): derive_status(risky_open_ports=[23], unexpected_open_ports=[], has_malicious_ip_match=False, has_bandwidth_anomaly=False) returns SecurityStatus.CRITICAL
    - Test 6 (test_security_status.py): derive_status(risky_open_ports=[], unexpected_open_ports=[8080], has_malicious_ip_match=False, has_bandwidth_anomaly=False) returns SecurityStatus.WARNING
    - Test 7 (test_security_status.py): derive_status(risky_open_ports=[], unexpected_open_ports=[], has_malicious_ip_match=False, has_bandwidth_anomaly=False) returns SecurityStatus.GOOD — this is the never-scanned-defaults-to-good case (RESEARCH.md Pitfall 3)
    - Test 8 (test_security_status.py): derive_status(risky_open_ports=[], unexpected_open_ports=[], has_malicious_ip_match=True, has_bandwidth_anomaly=False) returns SecurityStatus.CRITICAL (malicious-IP match alone is critical, no open ports needed)
    - Test 9 (test_threat_intel.py): StaticBlocklistSource loaded from a small fixture file (3-4 known CIDR lines written by the test itself, NOT the full vendored file) returns is_malicious("<ip-inside-a-fixture-CIDR>") == True
    - Test 10 (test_threat_intel.py): same fixture — is_malicious("8.8.8.8") (a deliberately-not-listed IP) returns False
    - Test 11 (test_threat_intel.py): is_malicious("not-an-ip") returns False (malformed input never raises)
  </behavior>
  <action>
    Create backend/src/services/port_rules.py exactly as specified in 04-RESEARCH.md Pattern 2 / 04-PATTERNS.md: module-level `RISKY_PORTS: frozenset[int]` containing {21, 23, 135, 139, 445, 512, 513, 514, 1433, 3306, 3389, 5900}; `EXPECTED_PORTS: dict[DeviceType, frozenset[int]]` with ROUTER={22,53,80,443}, IOT={80,443,1900}, TV={80,443,7000,8008,8009}, CONSOLE={80,443}, PHONE=frozenset(), LAPTOP=frozenset(), DESKTOP=frozenset(), TABLET=frozenset(), OTHER=frozenset(); function `evaluate_open_ports(device_type: DeviceType, open_ports: list[int]) -> tuple[list[int], list[int]]` returning (risky_open, unexpected_open). Import only `from src.models.device import DeviceType` — no SQLAlchemy/DB import in this file.

    Create backend/src/services/security_status.py with `class SecurityStatus(str, enum.Enum)` having members GOOD="good", WARNING="warning", CRITICAL="critical"; function `derive_status(*, risky_open_ports: list[int], unexpected_open_ports: list[int], has_malicious_ip_match: bool, has_bandwidth_anomaly: bool) -> SecurityStatus` implementing D-06's exact precedence: CRITICAL if risky_open_ports or has_malicious_ip_match; WARNING if unexpected_open_ports or has_bandwidth_anomaly; else GOOD. No DB import in this file either.

    Create backend/src/services/threat_intel_source.py: `class ThreatIntelSource(Protocol)` with method `is_malicious(self, ip: str) -> bool`; `class StaticBlocklistSource` whose `__init__(self, blocklist_path: str = "src/data/firehol_level1.netset")` opens the file, skips blank lines and lines starting with `#`, parses each remaining line with `ipaddress.ip_network(line, strict=False)` inside a try/except ValueError (skip malformed lines), storing into `self._networks: list[ipaddress.IPv4Network | ipaddress.IPv6Network]`; method `is_malicious(self, ip: str) -> bool` parses `ip` with `ipaddress.ip_address`, returns False on ValueError, otherwise returns `any(addr in net for net in self._networks)`. Add module-level `_DEFAULT_SOURCE: ThreatIntelSource | None = None` and `def get_default_threat_intel_source() -> ThreatIntelSource` implementing the lazy-singleton pattern from 04-RESEARCH.md's "Code Examples" section. Mirror `bandwidth_source.py`'s docstring convention: name `RemoteFeedSource` (D-10) as the future swap-in implementation in `StaticBlocklistSource`'s docstring.

    Fetch the real FireHOL `firehol_level1.netset` file from `https://raw.githubusercontent.com/firehol/blocklist-ipsets/master/firehol_level1.netset` and save it verbatim (preserving its own `# Date:`/`# Source File Date:` header lines per RESEARCH.md Pitfall 4) to `backend/src/data/firehol_level1.netset`. This is the production blocklist `StaticBlocklistSource` loads by default; it is NOT used by the unit tests in this task (tests use a small inline fixture file written by the test itself, per Test 9/10/11 above, for determinism and speed).

    Write the three pytest files using plain function calls only — no `test_db`/`client`/async fixtures, matching `domain_grouping.py`'s fixture-free unit-test style.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_port_rules.py tests/test_security_status.py tests/test_threat_intel.py -x -v</automated>
  </verify>
  <done>All 11 behavior cases pass; port_rules.py and security_status.py have zero imports from sqlalchemy or src.database; firehol_level1.netset exists under backend/src/data/ with its original header comments intact and at least 1000 CIDR lines.</done>
</task>

<task type="auto">
  <name>Task 2: Schema — Device columns, three new models, Alembic migration</name>
  <files>backend/src/models/device.py, backend/src/models/port_scan_result.py, backend/src/models/security_alert.py, backend/src/models/pending_scan_request.py, backend/alembic/versions/0005_security.py, backend/tests/conftest.py</files>
  <read_first>
    backend/src/models/device.py (current full content — modify in place, preserve DeviceType enum and all existing columns/docstring verbatim)
    backend/src/models/arp_event.py (simple autoincrement-PK + timestamp model shape for security_alert.py and pending_scan_request.py)
    backend/src/models/traffic_flow.py (history-row-per-event shape reference for port_scan_result.py, though port_scan_results uses a single autoincrement id PK, NOT a composite time+mac PK)
    backend/alembic/versions/0004_traffic_flows.py (most recent migration — copy its revision/down_revision/create_date header style and op.create_table conventions; this new migration's down_revision MUST be "0004")
    backend/tests/conftest.py (current model-import list at the top of test_db fixture — every new model module must be added to this explicit import list or Base.metadata.create_all will silently skip its table)
  </read_first>
  <action>
    Modify backend/src/models/device.py: add two new columns to the existing `Device` class (do not remove or rename any existing column) — `security_status: Mapped[str]` using `SAEnum(SecurityStatus, name="securitystatus", values_callable=lambda enum_cls: [e.value for e in enum_cls])`, `nullable=False`, `server_default=SecurityStatus.GOOD.value` (import `SecurityStatus` from `src.services.security_status`); `last_scanned_at: Mapped[datetime | None]` using `DateTime(timezone=True)`, `nullable=True` (no scan yet = None, surfaced by D-06's "not yet scanned" UI affordance, never via the status enum itself); `last_known_ip: Mapped[str | None]` using `String(45)`, `nullable=True` — Device currently tracks `last_known_mac` but no IP, and the capture container's on-demand/daily port scan (Plan 04-03) needs a current IP target, not a MAC. Set this column from `backend/src/services/discovery.py`'s `record_observation()` Device-branch (Plan 04-02 will wire the actual write) whenever an ARP observation carries a `src_ip`.

    Create backend/src/models/port_scan_result.py: `class PortScanResult(Base)`, `__tablename__ = "port_scan_results"`. Columns: `id` (autoincrement PK), `device_id: Mapped[int]` (`ForeignKey("devices.id")`, `nullable=False`), `scanned_at: Mapped[datetime]` (`DateTime(timezone=True)`, `nullable=False`, `server_default=func.now()`), `open_ports: Mapped[list[int]]` using SQLAlchemy's `JSON` type (portable across Postgres/SQLite — do not use Postgres-specific ARRAY since the SQLite test fixture must also support this column), `nullable=False`, `default=list`. This is a history table (one row per scan, per RESEARCH.md Open Question 1's recommendation) — do not give it a composite PK and do not register it as a hypertable.

    Create backend/src/models/security_alert.py: `class SecurityAlert(Base)`, `__tablename__ = "security_alerts"`. Columns per D-11: `id` (autoincrement PK), `device_id: Mapped[int | None]` (`ForeignKey("devices.id")`, `nullable=True` — unregistered/unknown-device alerts have no Device row), `type: Mapped[str]` using `SAEnum` with a new `class SecurityAlertType(str, enum.Enum)` defined in this same file with members `UNKNOWN_DEVICE="unknown_device"`, `MALICIOUS_IP="malicious_ip"`, `SUSPICIOUS_TRAFFIC="suspicious_traffic"`, `UNEXPECTED_PORT="unexpected_port"`; `severity: Mapped[str]` using the existing `SecurityStatus` SAEnum (import from `src.services.security_status` — reuse the same enum, do not create a parallel severity enum); `message: Mapped[str]` (`String(500)`, `nullable=False`); `created_at: Mapped[datetime]` (`DateTime(timezone=True)`, `nullable=False`, `server_default=func.now()`); `acknowledged: Mapped[bool]` (`Boolean`, `nullable=False`, `default=False`).

    Create backend/src/models/pending_scan_request.py: `class PendingScanRequest(Base)`, `__tablename__ = "pending_scan_requests"`. Columns: `id` (autoincrement PK), `device_id: Mapped[int]` (`ForeignKey("devices.id")`, `nullable=False`), `requested_at: Mapped[datetime]` (`DateTime(timezone=True)`, `nullable=False`, `server_default=func.now()`), `claimed_at: Mapped[datetime | None]` (`DateTime(timezone=True)`, `nullable=True` — set by the capture-side poll-and-claim in a later wave).

    Create backend/alembic/versions/0005_security.py with `revision = "0005"`, `down_revision = "0004"`. In `upgrade()`: `op.add_column("devices", sa.Column("security_status", sa.String(), nullable=False, server_default="good"))`, `op.add_column("devices", sa.Column("last_scanned_at", sa.TIMESTAMP(timezone=True), nullable=True))`; `op.add_column("devices", sa.Column("last_known_ip", sa.String(45), nullable=True))`; `op.create_table("port_scan_results", ...)` matching the model exactly (use `sa.JSON()` for open_ports); `op.create_table("security_alerts", ...)` matching the model exactly (device_id FK nullable=True); `op.create_table("pending_scan_requests", ...)` matching the model exactly. In `downgrade()`: drop the three new tables in reverse-dependency order, then `op.drop_column("devices", "last_known_ip")`, `op.drop_column("devices", "last_scanned_at")`, and `op.drop_column("devices", "security_status")`. These are plain relational tables (per STATE.md's note and RESEARCH.md's Architectural Responsibility Map) — do NOT call `create_hypertable` for any of them.

    Modify backend/tests/conftest.py: add `port_scan_result`, `security_alert`, `pending_scan_request` to the explicit model-import list inside the `test_db` fixture's import block (alongside the existing `app_settings, arp_event, bandwidth, device, ...` list) so the new tables are created by `Base.metadata.create_all` in tests.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_devices.py -x -v</automated>
  </verify>
  <done>backend/src/models/device.py has security_status (default "good"), last_scanned_at (nullable), and last_known_ip (nullable) columns; three new model files exist with the exact table/column shapes specified; 0005_security.py exists with revision="0005" and down_revision="0004", and creates all three new tables plus the two devices columns; existing test_devices.py suite still passes with the new Device columns present (proves no regression from the in-place model extension).</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts This Phase Produces

New symbols introduced by this plan (excluded from drift/source-grounding checks):

- `backend/src/services/port_rules.py`: `RISKY_PORTS`, `EXPECTED_PORTS`, `evaluate_open_ports(device_type, open_ports)`
- `backend/src/services/security_status.py`: `SecurityStatus` (enum: GOOD/WARNING/CRITICAL), `derive_status(*, risky_open_ports, unexpected_open_ports, has_malicious_ip_match, has_bandwidth_anomaly)`
- `backend/src/services/threat_intel_source.py`: `ThreatIntelSource` (Protocol), `StaticBlocklistSource`, `get_default_threat_intel_source()`
- `backend/src/models/port_scan_result.py`: `PortScanResult` model (table `port_scan_results`: id, device_id, scanned_at, open_ports)
- `backend/src/models/security_alert.py`: `SecurityAlert` model, `SecurityAlertType` enum (UNKNOWN_DEVICE/MALICIOUS_IP/SUSPICIOUS_TRAFFIC/UNEXPECTED_PORT) (table `security_alerts`: id, device_id, type, severity, message, created_at, acknowledged)
- `backend/src/models/pending_scan_request.py`: `PendingScanRequest` model (table `pending_scan_requests`: id, device_id, requested_at, claimed_at)
- `backend/src/models/device.py`: new columns `security_status`, `last_scanned_at`, `last_known_ip` on the existing `Device` class
- `backend/alembic/versions/0005_security.py`: migration revision "0005"
- `backend/src/data/firehol_level1.netset`: vendored static blocklist data file
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Vendored blocklist file → backend image | Build-time-bundled data file, not a runtime fetch — no new runtime trust boundary introduced |
| Pure-function services → DeviceType enum | Internal import only, no external input crosses this boundary in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|--------------|------------------|
| T-04-01 | Tampering | `backend/src/data/firehol_level1.netset` | accept | File is bundled at Docker build time from the backend repo (not fetched at runtime) — provenance is whatever the backend image's build process already guarantees; no new runtime-fetch attack surface (this is exactly what D-08's "zero external calls" constraint protects against) |
| T-04-02 | Information Disclosure | `port_scan_results.open_ports` (JSON column) | accept | Open-port data is inherently local-network-derived, no PII; same trust level as existing `traffic_flows` data already stored unencrypted in this single-user/household threat model |
| T-04-03 | Denial of Service | `StaticBlocklistSource.is_malicious()` linear scan | accept | ~4,500 CIDR entries is well within acceptable latency for a low-hundreds-of-distinct-IPs-per-rollup household workload (RESEARCH.md Assumption A4); optimization path exists (bisect over sorted ranges) without an interface change if ever needed |
| T-04-SC | Tampering | pip installs in this plan | accept | This plan adds zero new pip/npm dependencies — no package legitimacy gate applies here (the python-nmap fork dependency is introduced in a later wave's plan, where its own checkpoint applies) |
</threat_model>

<verification>
Run the full backend test suite to confirm no regression from the Device model extension: `cd backend && python -m pytest`. Confirm `evaluate_open_ports`, `derive_status`, and `StaticBlocklistSource.is_malicious` are all importable with zero `sqlalchemy`/`src.database` imports inside `port_rules.py`/`security_status.py` (grep for `import` lines in both files).
</verification>

<success_criteria>
- `backend/src/services/port_rules.py`, `security_status.py`, `threat_intel_source.py` exist, are framework-free pure-function/Protocol modules, and pass all 11 specified behavior tests.
- `backend/src/data/firehol_level1.netset` exists with the real FireHOL header preserved and ~4,000+ CIDR entries.
- `Device` model has `security_status` (defaults to "good") and `last_scanned_at` (nullable) columns.
- `PortScanResult`, `SecurityAlert`, `PendingScanRequest` models exist matching D-11's shape.
- Alembic migration `0005_security.py` applies cleanly on top of `0004` (verified structurally; full `alembic upgrade head` against real Postgres happens in CI/deployment, not in this plan's automated verify step since this dev environment has no live Postgres).
- Existing test suite (`test_devices.py` and the full suite) still passes — no regression from extending `Device`.
</success_criteria>

<output>
Create `.planning/phases/04-security/04-01-SUMMARY.md` when done
</output>
