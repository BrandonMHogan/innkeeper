---
phase: 02.1-device-identification-hints
plan: 02
subsystem: ui
tags: [svelte5, shadcn-svelte, bits-ui, popover, device-identification]

# Dependency graph
requires:
  - phase: 02.1-device-identification-hints (Plan 01)
    provides: "/api/devices/ unknown-identity payload with vendor/type_guess/name_guess/raw_signals, per 02.1-01-SUMMARY.md"
provides:
  - "shadcn-svelte popover component (frontend/src/lib/components/ui/popover/) wrapping bits-ui Popover primitive"
  - "Inference line ('Looks like: {Vendor} · {type}') on unknown DeviceCard, omitted entirely when no usable signal exists"
  - "Click/tap-to-toggle info-icon popover exposing raw MAC/vendor/mDNS/DHCP signal rows, gated on presence, 44x44px touch target, keyboard-operable"
  - "RegisterDialog nameGuess/typeGuess props pre-filling Name/Type fields with 'Guessed from device signals — feel free to change' captions"
  - "Dashboard +page.svelte wiring passing selected identity's name_guess/type_guess into RegisterDialog"
affects: ["future phases reading DeviceCard.svelte or RegisterDialog.svelte", "phase 02.1 Task 3 manual checkpoint (pending human verification)"]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "shadcn-svelte first-party component install (popover) reusing already-present bits-ui@2.18.1 transitive dependency — no package.json dependency bump, only new generated wrapper source files"
    - "Inline-style Svelte convention (no Tailwind/CSS modules) extended to new markup, consistent with DeviceCard.svelte/RegisterDialog.svelte's existing 100%-inline-style approach"
    - "Omit-entirely-rather-than-fabricate rendering pattern ({#if}-gated DOM nodes, no empty placeholders) applied to both the inference line and each raw-signal popover row"

key-files:
  created:
    - frontend/src/lib/components/ui/popover/index.ts
    - frontend/src/lib/components/ui/popover/popover.svelte
    - frontend/src/lib/components/ui/popover/popover-trigger.svelte
    - frontend/src/lib/components/ui/popover/popover-content.svelte
    - frontend/src/lib/components/ui/popover/popover-close.svelte
    - frontend/src/lib/components/ui/popover/popover-portal.svelte
    - frontend/src/lib/components/ui/popover/popover-header.svelte
    - frontend/src/lib/components/ui/popover/popover-title.svelte
    - frontend/src/lib/components/ui/popover/popover-description.svelte
  modified:
    - frontend/src/lib/components/DeviceCard.svelte
    - frontend/src/lib/components/RegisterDialog.svelte
    - frontend/src/routes/dashboard/+page.svelte
    - frontend/package.json

key-decisions:
  - "popover install required no package.json dependency change beyond adding the 'check' script — bits-ui@2.18.1 was already a transitive dependency from existing dialog/select components, confirmed via package.json diff in commit 493964d"
  - "Added a 'check' script (svelte-check) to package.json since the plan's verification command (npm run check) referenced a script that did not yet exist in the repo — Rule 3 blocking auto-fix, required to run the plan's own verification gate"
  - "Inference-line type labels use a dedicated lowercase/article-aware map (inferenceTypeLabelMap) distinct from RegisterDialog's Title Case Select option labels, per UI-SPEC's sentence-vs-dropdown casing distinction"

requirements-completed: [DISC-05, DISC-06]

# Metrics
duration: ~3min (verification-only this session; Tasks 1-2 implementation occurred in a prior session per existing commits 493964d/3d26b9a)
completed: 2026-06-19
---

# Phase 02.1 Plan 02: Frontend Inference Rendering Summary

**Inference line + raw-signal info popover on unknown DeviceCard, and Register dialog pre-fill from the same payload, built on shadcn-svelte's bits-ui Popover primitive — pure presentation, zero client-side inference logic.**

## Performance

- **Duration:** ~3 min (this session was a verification/continuation pass — Tasks 1 and 2 were already implemented and committed in a prior session, found at session start)
- **Started:** 2026-06-19T12:47:00Z (approx, per file mtimes and prior commit timestamps)
- **Completed:** 2026-06-19 (this verification pass)
- **Tasks:** 2 of 3 (Task 3 is a blocking human-verify checkpoint, not auto-executable)
- **Files modified:** 13 (9 created under `ui/popover/`, 4 modified: `DeviceCard.svelte`, `RegisterDialog.svelte`, `dashboard/+page.svelte`, `package.json`)

## Accomplishments
- Installed shadcn-svelte's `popover` component (Bits UI `Popover` primitive wrapper) into `frontend/src/lib/components/ui/popover/` — no new npm dependency, reuses the already-resolved `bits-ui@2.18.1`
- `DeviceCard.svelte`'s unknown-device branch now renders `Looks like: {Vendor} · {type}` (or vendor-only / type-only phrasing) exactly when `device.vendor` or `device.type_guess` is present, and renders nothing at all (no line, no icon) otherwise — matching UI-SPEC's "never fabricate" rule
- Info-icon trigger (14px `Info` glyph, 44×44px hit area, `aria-label="Show raw signal data"`) opens a click/tap-to-toggle popover listing raw MAC / raw vendor OUI string / mDNS service type / DHCP vendor class, each row present only when its value exists on `raw_signals`; falls back to "No raw signal data available." when none are present
- `RegisterDialog.svelte` accepts `nameGuess`/`typeGuess` props seeding the Name input and Type select's initial `$state`, with `resetForm()` re-seeding from the same props (not hardcoded empty) so reopening the dialog for the same identity restores the guess; matching "Guessed from device signals — feel free to change" captions render under each pre-filled field only
- `dashboard/+page.svelte` derives the selected identity's `name_guess`/`type_guess` and threads them into `RegisterDialog`
- `cd frontend && npm run check` passes with 0 errors (3 pre-existing/benign warnings: two `state_referenced_locally` Svelte 5 advisories on the guess-seeded `$state` initializers — expected and harmless since `resetForm()` is the actual re-seed mechanism, not a live `$derived`; one pre-existing `tsconfig.json` "Cannot find type definition for 'node'" warning unrelated to this plan)

## Task Commits

Each task was committed atomically (found already committed at session start, verified present in `git log`):

1. **Task 1: Install popover component and add inference line + info popover to DeviceCard** - `493964d` (feat)
2. **Task 2: Pre-fill Register dialog from inference and wire dashboard page** - `3d26b9a` (feat)

**Plan metadata:** (this commit, to follow)

## Files Created/Modified
- `frontend/src/lib/components/ui/popover/*.svelte`, `index.ts` - Generated shadcn-svelte Popover Root/Trigger/Content/Close/Portal/Header/Title/Description wrapper components on the Bits UI `Popover` primitive
- `frontend/src/lib/components/DeviceCard.svelte` - Extended `DeviceCardDevice` interface with `vendor`/`type_guess`/`name_guess`/`raw_signals`; added `inferenceLine` and `rawSignalRows` derived values; added the inference `<p>` + `Popover`/`PopoverTrigger`/`PopoverContent` markup in the unknown-device branch; added `.info-trigger` hover/focus-visible CSS for the icon color transition
- `frontend/src/lib/components/RegisterDialog.svelte` - Added `nameGuess`/`typeGuess` optional props; `name`/`type` `$state` now seed from these props instead of hardcoded empty; `resetForm()` re-seeds from props; added two "Guessed from device signals — feel free to change" helper captions, each gated on its respective guess prop being non-null
- `frontend/src/routes/dashboard/+page.svelte` - Extended `DeviceListItem` interface with `vendor`/`type_guess`/`name_guess`; added `selectedDeviceGuess` derived lookup; passes `nameGuess`/`typeGuess` to `<RegisterDialog>`
- `frontend/package.json` - Added a `check` script (`svelte-check --tsconfig ./tsconfig.json`) that the plan's verification step depends on but which did not previously exist in the repo

## Decisions Made
- No new npm dependency required for the popover install — `bits-ui@2.18.1` was already resolved as a transitive dependency via the existing `dialog`/`select` components; `package.json`'s only change was the addition of the `check` script
- Confirmed the `check` script addition was necessary (Rule 3 — blocking) because the plan's own verification command (`npm run check`) referenced a script absent from the existing `package.json`; without it, the plan's stated automated verification step could not run at all

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Added missing `check` script to package.json**
- **Found during:** Task 1 (verification step `cd frontend && npm run check`)
- **Issue:** The plan's `<verify>` step for both Task 1 and Task 2 specifies `cd frontend && npm run check`, but no `check` script existed in `frontend/package.json` prior to this plan — running it would have failed with "missing script" rather than running `svelte-check`
- **Fix:** Added `"check": "svelte-check --tsconfig ./tsconfig.json"` to `package.json`'s `scripts` block
- **Files modified:** `frontend/package.json`
- **Verification:** `cd frontend && npm run check` now runs successfully (0 errors, 3 pre-existing/benign warnings)
- **Committed in:** `493964d` (Task 1 commit)

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** Necessary for the plan's own verification gate to be runnable at all. No scope creep — script addition only, no behavior change.

## Issues Encountered
- This execution session began with Tasks 1 and 2 already fully implemented, committed (`493964d`, `3d26b9a`), and matching the plan's acceptance criteria exactly — confirmed via `git log`, file reads, and re-running all of the plan's specified `grep` acceptance checks plus `npm run check`. No rework was needed; this session's work was verification-only for Tasks 1-2, plus authoring this summary and surfacing Task 3's checkpoint.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- Tasks 1 and 2 are fully implemented, committed, and automated-verification-clean (`npm run check`: 0 errors)
- **Task 3 is a blocking `checkpoint:human-verify` gate and has NOT been approved yet.** Per the plan's `gate="blocking"` annotation and the execute-plan workflow, this plan does not advance past Task 3 without explicit human sign-off against the six-point manual verification checklist (see plan file `02.1-02-PLAN.md` Task 3, or the checkpoint message returned alongside this summary)
- STATE.md/ROADMAP.md/REQUIREMENTS.md updates and the final metadata commit are deliberately deferred until the human-verify checkpoint is approved, since DISC-05/DISC-06 are not yet confirmed complete until Task 3 passes

---
*Phase: 02.1-device-identification-hints*
*Completed (Tasks 1-2 only; Task 3 pending human verification): 2026-06-19*

## Self-Check: PASSED

All claimed files verified present on disk (`frontend/src/lib/components/ui/popover/*`, `DeviceCard.svelte`, `RegisterDialog.svelte`, `dashboard/+page.svelte`); both task commits (`493964d`, `3d26b9a`) verified present in `git log --oneline`; `npm run check` re-run and confirmed 0 errors; all plan-specified `grep` acceptance criteria re-run and confirmed matching.
