---
phase: 05-plugin-system-notifications
plan: 05
type: execute
wave: 4
depends_on: ["05-04"]
files_modified:
  - backend/src/modules/security/__init__.py
  - backend/src/modules/security/manifest.py
  - backend/src/modules/security/module.py
  - backend/src/modules/security/routes.py
  - backend/src/modules/security/models.py
  - backend/src/modules/security/migrations/0001_initial.py
  - backend/alembic.ini
  - backend/src/main.py
  - backend/src/routes/capture.py
  - backend/tests/conftest.py
  - frontend/src/lib/api.ts
autonomous: true
requirements: [MOD-07]
must_haves:
  truths:
    - "Security module owns its own Postgres schema (security.*, per D-11) via its own Alembic migrations/ directory and branch label (D-12) for port_scan_results, security_alerts, pending_scan_requests"
    - "Security's routes resolve device name/type for alert serialization through DeviceLookupInterface instead of a direct outerjoin on Device"
    - "POST /api/modules/security/scan/{device_id}, GET /scan/{device_id}, GET /alerts, POST /alerts/{id}/ack, POST /alerts/ack-all all return identical response shapes to their pre-retrofit versions"
    - "All five existing modules (device_identity, devices, traffic, security, linked_apps) boot together through a single ModuleLoader call in main.py with zero hardcoded app.include_router calls remaining for any of them"
  artifacts:
    - path: "backend/src/modules/security/models.py"
      provides: "PortScanResult, SecurityAlert, PendingScanRequest moved with schema=\"security\""
      contains: "schema.*security"
  key_links:
    - from: "backend/src/modules/security/routes.py"
      to: "backend/src/modules/device_identity/interfaces.py"
      via: "self._device_lookup.lookup(device_id) replacing the outerjoin(Device, ...) in list_alerts"
      pattern: "self\\._device_lookup\\.lookup\\("
    - from: "backend/src/main.py"
      to: "backend/src/host/loader.py"
      via: "single ModuleLoader call wiring device_identity, devices, traffic, security, linked_apps together"
      pattern: "ModuleLoader|build_load_order"
---

<objective>
Retrofit Security (`routes/security.py`, `models/port_scan_result.py`, `models/security_alert.py`, `models/pending_scan_request.py`) into a feature module with its own `security` Postgres schema, same retrofit pattern as Plan 04's Traffic — replace the `outerjoin(Device, ...)` in `list_alerts` with a `DeviceLookupInterface.lookup()` call. This is the last of the three feature-module retrofits; after this plan, `main.py` boots every module (device_identity, devices, traffic, security, linked_apps) through a single `ModuleLoader` call with zero hardcoded `app.include_router` calls remaining for retrofitted code.

Purpose: Complete MOD-07's full retrofit scope and prove the module contract holds across all three originally-bolt-on feature areas, closing out the highest-regression-risk portion of the phase.
Output: `backend/src/modules/security/` package, `security` Postgres schema via its own Alembic branch, `main.py` fully ModuleLoader-driven, zero regressions in the existing security/alerts test suite.
</objective>

<artifacts_produced>
## Artifacts this phase produces

- `backend/src/modules/security/models.py` — `PortScanResult`, `SecurityAlert`, `SecurityAlertType`, `PendingScanRequest` (relocated, schema-qualified to `security`)
- `backend/src/modules/security/routes.py` — relocated router with `self._device_lookup.lookup()` call sites (new)
- `backend/src/modules/security/module.py`, `manifest.py` — new module package (`MANIFEST` with `kind="feature"`, `requires=[DeviceLookupInterface]`, `db_schema="security"`)
- `backend/src/modules/security/migrations/0001_initial.py` — new Alembic branch revision, `branch_labels=("security",)`
- `/api/modules/security/` — new canonical route prefix (replaces `/api/security/`)
- `backend/src/main.py` — single consolidated `ModuleLoader` call wiring all five modules (final state: only `auth` + `capture` remain hardcoded)
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-plugin-system-notifications/05-CONTEXT.md
@.planning/phases/05-plugin-system-notifications/05-RESEARCH.md
@.planning/phases/05-plugin-system-notifications/05-PATTERNS.md
@.planning/phases/05-plugin-system-notifications/05-03-SUMMARY.md
@.planning/phases/05-plugin-system-notifications/05-04-SUMMARY.md
@backend/src/routes/security.py
@backend/src/models/port_scan_result.py
@backend/src/models/security_alert.py
@backend/src/models/pending_scan_request.py
@backend/alembic/versions/0005_security.py
@backend/src/main.py
@backend/src/host/dependencies.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Security module — models, schema-qualified migration, DeviceLookupInterface retrofit, final main.py consolidation</name>
  <files>backend/src/modules/security/__init__.py, backend/src/modules/security/manifest.py, backend/src/modules/security/module.py, backend/src/modules/security/routes.py, backend/src/modules/security/models.py, backend/src/modules/security/migrations/0001_initial.py, backend/alembic.ini, backend/src/main.py, backend/src/routes/capture.py, backend/tests/conftest.py</files>
  <read_first>
    backend/src/routes/security.py (full file — moved into security/routes.py; `list_alerts`'s `select(SecurityAlert, Device.name).outerjoin(Device, Device.id == SecurityAlert.device_id)` at lines 89-92 is replaced — the join becomes a per-alert `self._device_lookup.lookup(alert.device_id)` call when `alert.device_id` is not None, else `device_name=None` exactly as the outerjoin would yield for an unregistered identity's alert), backend/src/models/port_scan_result.py, backend/src/models/security_alert.py, backend/src/models/pending_scan_request.py (all three moved verbatim into security/models.py, add schema="security"; note PendingScanRequest and PortScanResult both FK to devices.id — these FKs become schema-qualified ForeignKey("device_identity.devices.id") since Device now lives in device_identity per Plan 03), backend/alembic/versions/0005_security.py (full file — read the exact column/table definitions for port_scan_results/security_alerts/pending_scan_requests being relocated), backend/src/main.py (current state after Plans 03/04 — read the actual ModuleLoader call structure those plans established; this task adds security + linked_apps into the same call and removes any remaining hardcoded app.include_router for the security router specifically), backend/src/routes/capture.py (PortScanResult/SecurityAlert/PendingScanRequest imports need updating to src.modules.security.models; also re-check the malicious-IP-check / bandwidth-anomaly alert-writing call sites in capture.py for the same import-path fix), backend/src/host/dependencies.py (Plan 01's real require_module_enabled/is_module_enabled signatures — import from here for this task's Depends(require_module_enabled("security")) stack)
  </read_first>
  <behavior>
    - Test 1 (existing test_security_alerts.py / equivalent — re-run unmodified): `GET /api/modules/security/alerts` returns identical response shape including `device_name` resolved correctly for both a registered-device alert and a `device_id=None` unknown-device alert.
    - Test 2 (existing): `POST /api/modules/security/scan/{device_id}` still returns 202 with the same response body; `GET /api/modules/security/scan/{device_id}` still returns the same risky/unexpected/expected port-flag shape.
    - Test 3 (existing): `POST /api/modules/security/alerts/{id}/ack` and `/alerts/ack-all` still behave identically.
    - Test 4 (new): after this task, `main.py` contains exactly one `ModuleLoader`/loader-invocation call site wiring all five modules (device_identity, devices, traffic, security, linked_apps) — grep-verified, confirming zero leftover hardcoded `app.include_router` calls for any of these five.
  </behavior>
  <action>
    Create `backend/src/modules/security/models.py` moving `PortScanResult`, `SecurityAlert`, `SecurityAlertType`, `PendingScanRequest` verbatim from their three source files, adding `__table_args__ = {"schema": "security"}` to each, and updating any `ForeignKey("devices.id")` to `ForeignKey("device_identity.devices.id")`. Create `backend/src/modules/security/routes.py` moving `routes/security.py`'s router wholesale; in `list_alerts`, replace the `select(SecurityAlert, Device.name).outerjoin(Device, ...)` query with `select(SecurityAlert)` alone, then for each alert with `device_id is not None` call `await self._device_lookup.lookup(str(alert.device_id))` to get the device name for `_serialize_alert`'s `device_name` argument (alerts with `device_id is None` keep `device_name=None` exactly as the outerjoin would have produced); similarly replace `trigger_scan`'s and `get_scan_result`'s `select(Device).where(Device.id == device_id)` existence checks with `self._device_lookup.lookup(str(device_id))` returning `None`/not-found to drive the existing 404 `HTTPException`. Add `Depends(require_module_enabled("security"))` stacked with `Depends(require_auth)` on every route. Create `backend/src/modules/security/module.py` with `create(deps: dict[type, object]) -> SecurityModule` factory receiving `deps[DeviceLookupInterface]`, exposing `get_router()`. Create `backend/src/modules/security/manifest.py` with `MANIFEST = ModuleManifest(id="security", display_name="Security", version="1.0.0", kind="feature", provides=[], requires=[DeviceLookupInterface], db_schema="security")`. Update `backend/alembic.ini`'s `version_locations` adding `%(here)s/src/modules/security/migrations`. Create `backend/src/modules/security/migrations/0001_initial.py` with `down_revision` pointing at `traffic`'s `0001_initial` revision id (chains after Plan 04): `CREATE SCHEMA IF NOT EXISTS security`, `ALTER TABLE port_scan_results SET SCHEMA security`, `ALTER TABLE security_alerts SET SCHEMA security`, `ALTER TABLE pending_scan_requests SET SCHEMA security`. In `backend/src/main.py`, add `security`'s and `linked_apps`' (from Plan 02) manifests/factories into the single `ModuleLoader` call already established by Plans 03/04, removing the last hardcoded `from src.routes import security` import and its `app.include_router` call — confirm `main.py` now imports `auth` and `capture` as the only two hardcoded routers remaining (these stay outside the module system per CONTEXT.md scope — auth/capture are host-level concerns, not modules). Update `backend/src/routes/capture.py`'s `PortScanResult`/`SecurityAlert`/`PendingScanRequest` imports to `src.modules.security.models`. Update `frontend/src/lib/api.ts`'s `triggerScan()`/`listAlerts()`/`ackAlert()`/`ackAllAlerts()`/`getScanResult()` to call `/api/modules/security/...` instead of `/api/security/...`.
  </action>
  <acceptance_criteria>
    - `grep -c "outerjoin(Device" backend/src/modules/security/routes.py` equals 0
    - `grep -c "self\._device_lookup\.lookup" backend/src/modules/security/routes.py` is at least 2
    - `grep -c "require_module_enabled" backend/src/modules/security/routes.py` is at least 1 per route (5 routes minimum)
    - `grep -c "app.include_router" backend/src/main.py` equals 2 (auth + capture only — devices/traffic/security/linked_apps all go through the loader)
    - `grep -c "/api/security/" frontend/src/lib/api.ts` equals 0
    - full backend suite green, zero regressions
  </acceptance_criteria>
  <verify>
    <automated>cd backend && /tmp/innkeeper-venv313/bin/python -m pytest</automated>
  </verify>
  <done>Security module owns its own schema, resolves device names exclusively through DeviceLookupInterface, main.py mounts auth+capture as the only two hardcoded routers with everything else loader-driven, full suite green, behavior parity confirmed for every security/alerts route.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Security routes → DeviceLookupInterface | In-process Protocol boundary replacing the outerjoin on Device |
| Capture container → /api/capture/scan, /pending-scans, /queue-daily-scans (unchanged HTTP contract, relocated schema) | Same external contract, internal schema relocation only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-14 | Tampering | Security module bypassing DeviceLookupInterface to outerjoin Device directly | mitigate | Acceptance criterion greps security/routes.py for zero outerjoin(Device usage |
| T-05-15 | Elevation of Privilege | Disabled security module's scan/alert routes still reachable | mitigate | Depends(require_module_enabled("security")) stacked on every route |
| T-05-16 | Information Disclosure | Alert's device_name resolution leaking across schema boundary if DeviceLookupInterface implementation queries beyond its own schema | accept | DeviceLookupInterface's implementation lives entirely inside device_identity (Plan 03), already audited there; security module itself never queries device_identity.* directly |
</threat_model>

<verification>
Full backend suite green. Manual: confirm via the Lima VM docker-compose stack that a live port scan and a live malicious-IP alert both still populate `security.port_scan_results`/`security.security_alerts` correctly and the dashboard's SecurityAlertsBanner/ScanResultDialog still render real data (mirrors the Phase 3 P04 / Phase 4 P04 live-verification precedent).
</verification>

<success_criteria>
- Security is a feature module owning the `security` schema, mounted via ModuleLoader at `/api/modules/security/`
- DeviceLookupInterface fully replaces the outerjoin(Device) pattern
- main.py mounts only auth+capture as hardcoded routers; everything else (device_identity, devices, traffic, security, linked_apps) is loader-driven
- Zero regressions in existing security/alerts tests
</success_criteria>

<output>
Create `.planning/phases/05-plugin-system-notifications/05-05-SUMMARY.md` when done
</output>
