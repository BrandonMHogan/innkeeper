---
phase: 05-plugin-system-notifications
plan: 04
type: execute
wave: 3
depends_on: ["05-03"]
files_modified:
  - backend/src/modules/traffic/__init__.py
  - backend/src/modules/traffic/manifest.py
  - backend/src/modules/traffic/module.py
  - backend/src/modules/traffic/routes.py
  - backend/src/modules/traffic/models.py
  - backend/src/modules/traffic/broadcaster.py
  - backend/src/modules/traffic/migrations/0001_initial.py
  - backend/alembic.ini
  - backend/src/main.py
  - backend/src/routes/capture.py
  - backend/tests/conftest.py
  - frontend/src/lib/api.ts
autonomous: true
requirements: [MOD-05, MOD-07]
must_haves:
  truths:
    - "Traffic module owns its own Postgres schema (traffic.*, per D-11) via its own Alembic migrations/ directory and branch label (D-12) for traffic_flows and bandwidth_metrics, schema-qualified per D-14"
    - "Traffic's routes no longer query Device/DeviceMacHistory directly — device-MAC resolution goes through DeviceLookupInterface.lookup()"
    - "GET /api/modules/traffic/stream (SSE), /bandwidth/network, /bandwidth/{device_id}, /devices/{device_id}/destinations all return identical response shapes to their pre-retrofit versions"
    - "traffic_broadcaster's background loop runs as a HasCollector-wired module, started/stopped by ModuleLoader instead of main.py's hardcoded lifespan block"
  artifacts:
    - path: "backend/src/modules/traffic/broadcaster.py"
      provides: "update_snapshot_loop wrapped to satisfy HasCollector.run_collector(stop_event)"
      contains: "async def run_collector"
    - path: "backend/src/modules/traffic/migrations/0001_initial.py"
      provides: "traffic schema + schema-qualified traffic_flows hypertable + bandwidth_metrics + continuous aggregates"
      contains: "CREATE SCHEMA IF NOT EXISTS traffic"
  key_links:
    - from: "backend/src/modules/traffic/routes.py"
      to: "backend/src/modules/device_identity/interfaces.py"
      via: "self._device_lookup.lookup(device_id) replacing _resolve_device_macs' direct Device/DeviceMacHistory joins"
      pattern: "self\\._device_lookup\\.lookup\\("
    - from: "backend/src/main.py"
      to: "backend/src/modules/traffic/module.py"
      via: "ModuleLoader instantiates traffic module with requires=[DeviceLookupInterface] resolved at startup"
      pattern: "traffic"
---

<objective>
Retrofit Traffic (`routes/traffic.py`, `models/traffic_flow.py`, `models/bandwidth.py`, `services/traffic_broadcaster.py`) into a feature module with its own `traffic` Postgres schema, replacing `_resolve_device_macs`'s direct `Device`/`DeviceMacHistory` joins with a call through `DeviceLookupInterface` (constructor-injected from Plan 03's DeviceIdentity module). The background snapshot-broadcaster loop becomes a `HasCollector`-wired module capability, started/stopped by `ModuleLoader` instead of `main.py`'s hardcoded lifespan block.

Purpose: Prove MOD-05 (collector capability) and MOD-07 (Traffic as a thin DeviceLookupInterface consumer) on the second-highest-traffic code path in the system (the SSE-fed live feed), one module retrofit at a time per the Risk Summary's explicit ask.
Output: `backend/src/modules/traffic/` package, `traffic` Postgres schema via its own Alembic branch, `traffic_flows`/`bandwidth_metrics` relocated with schema-qualified `create_hypertable()`, zero regressions in the existing traffic test suite.
</objective>

<artifacts_produced>
## Artifacts this phase produces

- `backend/src/modules/traffic/models.py` — `TrafficFlow`, `BandwidthMetric` (relocated, schema-qualified to `traffic`)
- `backend/src/modules/traffic/broadcaster.py` — `run_collector(stop_event)` (new `HasCollector`-satisfying wrapper around relocated `update_snapshot_loop`/`get_latest_snapshot`)
- `backend/src/modules/traffic/routes.py` — relocated router with `self._device_lookup.lookup()` call sites (new)
- `backend/src/modules/traffic/module.py`, `manifest.py` — new module package (`MANIFEST` with `kind="feature"`, `requires=[DeviceLookupInterface]`, `db_schema="traffic"`)
- `backend/src/modules/traffic/migrations/0001_initial.py` — new Alembic branch revision, `branch_labels=("traffic",)`
- `/api/modules/traffic/` — new canonical route prefix (replaces `/api/traffic/`)
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-plugin-system-notifications/05-CONTEXT.md
@.planning/phases/05-plugin-system-notifications/05-RESEARCH.md
@.planning/phases/05-plugin-system-notifications/05-PATTERNS.md
@.planning/phases/05-plugin-system-notifications/05-03-SUMMARY.md
@backend/src/routes/traffic.py
@backend/src/services/traffic_broadcaster.py
@backend/src/models/traffic_flow.py
@backend/src/models/bandwidth.py
@backend/alembic/versions/0004_traffic_flows.py
@backend/src/main.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Traffic module — models, schema-qualified migration, DeviceLookupInterface retrofit, HasCollector-wrapped broadcaster</name>
  <files>backend/src/modules/traffic/__init__.py, backend/src/modules/traffic/manifest.py, backend/src/modules/traffic/module.py, backend/src/modules/traffic/routes.py, backend/src/modules/traffic/models.py, backend/src/modules/traffic/broadcaster.py, backend/src/modules/traffic/migrations/0001_initial.py, backend/alembic.ini, backend/src/main.py, backend/src/routes/capture.py, backend/tests/conftest.py</files>
  <read_first>
    backend/src/routes/traffic.py (full file — moved into traffic/routes.py; `_resolve_device_macs` lines 46-67 is deleted entirely, its logic moves into device_identity's DeviceLookupInterface.lookup() implementation per Plan 03 Task 1 — read that implementation's actual shape from .planning/phases/05-plugin-system-notifications/05-03-SUMMARY.md before wiring the call site here), backend/src/services/traffic_broadcaster.py (full file, especially lines 94-114's stop_event/asyncio.wait_for loop — wrapped as run_collector(stop_event), not rewritten), backend/src/models/traffic_flow.py and backend/src/models/bandwidth.py (moved verbatim into traffic/models.py, add schema="traffic"), backend/alembic/versions/0004_traffic_flows.py (full file — the hypertable/continuous-aggregate/compression-policy migration being re-created schema-qualified; read every create_hypertable/add_compression_policy/add_continuous_aggregate_policy call site), backend/src/main.py (current lifespan block creating the broadcaster_stop_event/broadcaster_task directly — this task removes that hardcoded block once ModuleLoader's HasCollector wiring from Plan 01 takes over), backend/src/routes/capture.py (any import of traffic_flow/bandwidth models needs updating to the new traffic.models path — capture.py writes raw TrafficFlow/BandwidthMetric rows on ingest, this is a transitional import-path fix like Plan 03 Task 2's capture.py/security.py shim)
  </read_first>
  <behavior>
    - Test 1 (existing test_traffic_destinations.py / test_bandwidth_*.py — re-run unmodified): `GET /api/modules/traffic/devices/{device_id}/destinations` returns identical response shape to the pre-retrofit `/api/traffic/devices/{device_id}/destinations`.
    - Test 2 (existing): `GET /api/modules/traffic/bandwidth/{device_id}` and `/bandwidth/network` return identical shapes; MAC-rotation-aware aggregation (the `seeded_traffic_db` fixture's two-MAC device) still resolves correctly through `DeviceLookupInterface.lookup()` instead of `_resolve_device_macs`.
    - Test 3 (new): a `HasCollector`-satisfying traffic module instance's `run_collector(stop_event)` is the exact `update_snapshot_loop` function (or a thin wrapper calling it) — setting `stop_event` causes the loop to exit within one `wait_for` cycle (test with a short timeout override or by asserting the loop checks `stop_event.is_set()` before each iteration, matching the existing loop's structure).
    - Test 4 (existing): `GET /api/modules/traffic/stream` (SSE) still returns `EventSourceResponse` content-type and the same snapshot JSON shape from `get_latest_snapshot()`.
  </behavior>
  <action>
    Create `backend/src/modules/traffic/models.py` moving `TrafficFlow`, `BandwidthMetric` verbatim from `backend/src/models/{traffic_flow,bandwidth}.py`, adding `__table_args__ = {"schema": "traffic"}` (or equivalent dict merge if any existing `__table_args__` exists — check both files first) to each. Create `backend/src/modules/traffic/broadcaster.py` moving `update_snapshot_loop` and `get_latest_snapshot`/`_latest_snapshot`/`_compute_snapshot` verbatim from `backend/src/services/traffic_broadcaster.py`, adding a thin `async def run_collector(self, stop_event: asyncio.Event) -> None: await update_snapshot_loop(stop_event, self._session_factory)` method on the module instance (or rename `update_snapshot_loop` itself to `run_collector` if that's cleaner — Claude's Discretion, document the choice) so it satisfies `HasCollector` — the loop's internal `stop_event.is_set()`/`asyncio.wait_for(stop_event.wait(), timeout=7)` mechanics must not change at all. Create `backend/src/modules/traffic/routes.py` moving `routes/traffic.py`'s router wholesale; delete `_resolve_device_macs` entirely; every call site that used it (`device_bandwidth`, `device_destinations`) now calls `await self._device_lookup.lookup(device_id)` and reads the `.macs` set off the returned `DeviceInfo` (per Plan 03's `DeviceLookupInterface` contract — read the actual field name from `05-03-SUMMARY.md`); add `Depends(require_module_enabled("traffic"))` stacked with the existing `Depends(require_auth)` on every route. Create `backend/src/modules/traffic/module.py` with a `create(deps: dict[type, object]) -> TrafficModule` factory receiving `deps[DeviceLookupInterface]`, exposing `get_router()` (HasAPIRoutes) and `run_collector(stop_event)` (HasCollector). Create `backend/src/modules/traffic/manifest.py` with `MANIFEST = ModuleManifest(id="traffic", display_name="Traffic", version="1.0.0", kind="feature", provides=[], requires=[DeviceLookupInterface], db_schema="traffic")`. Update `backend/alembic.ini`'s `version_locations` adding `%(here)s/src/modules/traffic/migrations`. Create `backend/src/modules/traffic/migrations/0001_initial.py` with `down_revision` pointing at `device_identity`'s `0001_initial` revision id (chain after Plan 03, per RESEARCH.md Pattern 7/8): `CREATE SCHEMA IF NOT EXISTS traffic`, `ALTER TABLE traffic_flows SET SCHEMA traffic`, `ALTER TABLE bandwidth_metrics SET SCHEMA traffic` (data migration moving existing rows), then re-point the existing continuous aggregates (`bandwidth_daily`/`bandwidth_weekly`/`bandwidth_monthly` from `0004_traffic_flows.py`) at the schema-qualified table name — read `0004_traffic_flows.py`'s exact `create_hypertable`/`add_compression_policy`/`add_continuous_aggregate_policy` calls and re-issue them schema-qualified (`'traffic.traffic_flows'`) only if the existing hypertable registration needs to be redone after the `ALTER TABLE ... SET SCHEMA` (verify via TimescaleDB docs/RESEARCH.md Pattern 8 whether `SET SCHEMA` on an existing hypertable carries its TimescaleDB metadata automatically — if uncertain, the safer path is dropping and recreating the continuous aggregates after the schema move, documented explicitly in this migration's docstring). In `backend/src/main.py`, add `traffic`'s manifest/factory into the same `ModuleLoader` call introduced in Plan 03, remove the hardcoded `broadcaster_stop_event`/`broadcaster_task` creation from `lifespan()` (now handled by the loader's `HasCollector` wiring), remove `from src.routes import traffic` and its `app.include_router` call (mounted via the loader at `/api/modules/traffic/` instead). Update `backend/src/routes/capture.py`'s `TrafficFlow`/`BandwidthMetric` imports to `src.modules.traffic.models`. Update `frontend/src/lib/api.ts`'s `openTrafficStream()`/`getDeviceBandwidth()`/`getNetworkBandwidth()`/`getDeviceDestinations()` to call `/api/modules/traffic/...` instead of `/api/traffic/...`.
  </action>
  <acceptance_criteria>
    - `grep -c "_resolve_device_macs" backend/src/modules/traffic/routes.py` equals 0
    - `grep -c "self\._device_lookup\.lookup" backend/src/modules/traffic/routes.py` is at least 2
    - `grep -c "require_module_enabled" backend/src/modules/traffic/routes.py` is at least 1 per route (4 routes minimum)
    - `grep -c "/api/traffic/" frontend/src/lib/api.ts` equals 0
    - existing traffic test files (`test_traffic_destinations.py`, `test_bandwidth_*.py` or equivalent) pass unmodified in assertions against the new `/api/modules/traffic/` path
    - full backend suite green, zero regressions
  </acceptance_criteria>
  <verify>
    <automated>cd backend && /tmp/innkeeper-venv313/bin/python -m pytest</automated>
  </verify>
  <done>Traffic module owns its own schema, resolves device MACs exclusively through DeviceLookupInterface, the broadcaster loop runs as a HasCollector wired by ModuleLoader (not main.py's hardcoded lifespan block), SSE/bandwidth/destinations routes are behavior-identical at the new /api/modules/traffic/ prefix, full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Traffic routes → DeviceLookupInterface | In-process Protocol boundary replacing direct Device/DeviceMacHistory joins |
| Capture container → TrafficFlow/BandwidthMetric ingest (unchanged HTTP contract, relocated schema) | Same external contract, internal schema relocation only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-11 | Tampering | Traffic module bypassing DeviceLookupInterface to query device_identity.* directly | mitigate | Acceptance criterion greps traffic/routes.py for zero `_resolve_device_macs`/direct Device queries; only `self._device_lookup.lookup()` calls remain |
| T-05-12 | Elevation of Privilege | Disabled traffic module's SSE/bandwidth routes still reachable | mitigate | Depends(require_module_enabled("traffic")) stacked on every route per Pattern 9 |
| T-05-13 | Denial of Service | Continuous-aggregate re-registration after ALTER TABLE...SET SCHEMA silently fails to refresh, leaving network-wide bandwidth charts stale | mitigate | Migration's docstring documents the verified TimescaleDB behavior (recreate vs. carries-over) per the action's explicit instruction; behavior-parity test for /bandwidth/network confirms data is queryable post-migration |
</threat_model>

<verification>
Full backend suite green. Manual: confirm via the Lima VM docker-compose stack (per Pitfall 3/Phase 3 P04 precedent) that live traffic ingest still populates `traffic.traffic_flows`/`traffic.bandwidth_metrics` correctly after the schema move, and the SSE feed and bandwidth charts render with real data.
</verification>

<success_criteria>
- Traffic is a feature module owning the `traffic` schema, mounted via ModuleLoader at `/api/modules/traffic/`
- DeviceLookupInterface fully replaces `_resolve_device_macs`
- traffic_broadcaster's loop runs as a HasCollector, started/stopped by ModuleLoader
- Zero regressions in existing traffic/bandwidth tests
</success_criteria>

<output>
Create `.planning/phases/05-plugin-system-notifications/05-04-SUMMARY.md` when done
</output>
