---
phase: 05-plugin-system-notifications
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/modules/__init__.py
  - backend/src/modules/linked_apps/__init__.py
  - backend/src/modules/linked_apps/manifest.py
  - backend/src/modules/linked_apps/linked_manifest.py
  - backend/src/modules/linked_apps/routes.py
  - backend/tests/test_linked_apps.py
  - frontend/src/lib/styles/theme.css
  - frontend/src/lib/components/LinkedAppsSection.svelte
  - frontend/src/lib/api.ts
autonomous: true
requirements: [MOD-08]
must_haves:
  truths:
    - "A LinkedModuleManifest (id, name, icon_url, target_url) data shape exists and can be listed via an API endpoint"
    - "The dashboard shows a 'Linked Apps' section using the same card-grid pattern as the Devices grid, rendering the 'No linked apps yet' empty state when zero entries exist"
    - "Design tokens (CSS variables in theme.css) are confirmed complete and documented as the single source of truth per D-18 — no module-specific token duplication is introduced (D-19/D-20: shared component library is the default, convention not runtime enforcement; D-21: no module federation, single SvelteKit bundle)"
  artifacts:
    - path: "backend/src/modules/linked_apps/linked_manifest.py"
      provides: "LinkedModuleManifest Pydantic model (id, name, icon_url, target_url)"
      contains: "class LinkedModuleManifest"
    - path: "backend/src/modules/linked_apps/routes.py"
      provides: "GET /api/modules/linked-apps/ returning the configured linked-app list (empty list in v1)"
      exports: ["router"]
    - path: "frontend/src/lib/components/LinkedAppsSection.svelte"
      provides: "Card-grid + empty-state rendering for Linked Apps, reusing the Devices grid's CSS pattern"
      min_lines: 20
  key_links:
    - from: "frontend/src/lib/components/LinkedAppsSection.svelte"
      to: "backend/src/modules/linked_apps/routes.py"
      via: "fetch GET /api/modules/linked-apps/"
      pattern: "linked-apps"
---

<objective>
Build the Linked Apps data model + dashboard section (MOD-08) as a self-contained, manifest-only module package, and consolidate the frontend design-token/component-library convention (D-18–D-22) ahead of the Devices/Traffic/Security UI retrofit that begins in Plan 03. This plan has zero dependency on Plan 01's host infrastructure — `linked_apps` is intentionally simple enough to ship its own router directly without going through `ModuleLoader` yet (the loader wiring of this router into `/api/modules/linked-apps/` happens implicitly once Plan 03 wires `main.py` through the loader; until then this plan mounts it directly in `main.py` at the same prefix so the route is live and testable now).

Purpose: Prove the "manifest format + empty Linked Apps section" success criterion (Roadmap Phase 5 criterion 6) independently of the higher-risk retrofit work, and lock down the shared design-token/component convention before any retrofitted markup lands.
Output: `backend/src/modules/linked_apps/` package (manifest, LinkedModuleManifest, routes), passing `test_linked_apps.py`, `frontend/src/lib/components/LinkedAppsSection.svelte` rendering the empty state on the dashboard, and a verified-complete `theme.css` token set.
</objective>

<artifacts_produced>
## Artifacts this phase produces

- `backend/src/modules/__init__.py`, `backend/src/modules/linked_apps/__init__.py` — new `modules` package + `linked_apps` subpackage
- `backend/src/modules/linked_apps/linked_manifest.py` — `LinkedModuleManifest` (new Pydantic model: `id`, `name`, `icon_url`, `target_url`)
- `backend/src/modules/linked_apps/manifest.py` — `MANIFEST` (`ModuleManifest` instance for `linked_apps`)
- `backend/src/modules/linked_apps/routes.py` — `router` with `GET /` (new route, mounted at `/api/modules/linked-apps`)
- `frontend/src/lib/components/LinkedAppsSection.svelte` — new Svelte component
- `frontend/src/lib/api.ts` — `getLinkedApps()` (new function)
- `backend/tests/test_linked_apps.py` — new test file
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-plugin-system-notifications/05-CONTEXT.md
@.planning/phases/05-plugin-system-notifications/05-UI-SPEC.md
@.planning/phases/05-plugin-system-notifications/05-PATTERNS.md
@backend/src/main.py
@backend/src/routes/devices.py
@frontend/src/lib/api.ts
@frontend/src/lib/styles/theme.css
@frontend/src/routes/dashboard/+page.svelte
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: LinkedModuleManifest model, routes, and tests</name>
  <files>backend/src/modules/__init__.py, backend/src/modules/linked_apps/__init__.py, backend/src/modules/linked_apps/manifest.py, backend/src/modules/linked_apps/linked_manifest.py, backend/src/modules/linked_apps/routes.py, backend/tests/test_linked_apps.py, backend/src/main.py</files>
  <read_first>
    backend/src/routes/devices.py (router/Depends(require_auth) convention to replicate exactly), backend/src/main.py (current router-mounting block — this task adds one more app.include_router call), backend/src/host/manifest.py (written in Plan 01 — read its actual ModuleManifest shape to construct linked_apps' own manifest.py instance correctly; if Plan 01 has not landed yet in this execution order, use the exact ModuleManifest field shape from CONTEXT.md D-06 as a fallback)
  </read_first>
  <behavior>
    - Test 1 (test_linked_apps.py): `GET /api/modules/linked-apps/` (authenticated) returns `200` with an empty JSON list when no linked apps are configured.
    - Test 2 (test_linked_apps.py): `LinkedModuleManifest(id="jellyfin", name="Jellyfin", icon_url="https://example.com/icon.png", target_url="https://jellyfin.local")` round-trips through `.model_dump()` preserving all four fields exactly.
    - Test 3 (test_linked_apps.py): `GET /api/modules/linked-apps/` without authentication returns `401` (mirrors every other route's `require_auth` convention).
  </behavior>
  <action>
    Create `backend/src/modules/__init__.py` (empty package marker). Create `backend/src/modules/linked_apps/linked_manifest.py` with `LinkedModuleManifest(BaseModel)`: fields `id: str`, `name: str`, `icon_url: str`, `target_url: str` — per D-23, no code beyond this data shape. Create `backend/src/modules/linked_apps/manifest.py` with a `MANIFEST = ModuleManifest(id="linked_apps", display_name="Linked Apps", version="1.0.0", kind="feature", provides=[], requires=[], db_schema=None)` instance documenting this module's eventual home in the ModuleLoader registration sequence (Plan 03+ wires it through the loader; this plan does not call the loader). Create `backend/src/modules/linked_apps/routes.py` with `router = APIRouter()` and `GET /` returning `[]` (v1 ships zero real linked apps per D-23 — this endpoint exists so the frontend has a real contract to call, not a hardcoded frontend empty array), gated behind `_: None = Depends(require_auth)` exactly like `routes/devices.py`. In `backend/src/main.py`, add `from src.modules.linked_apps import routes as linked_apps_routes` and `app.include_router(linked_apps_routes.router, prefix="/api/modules/linked-apps")` alongside the existing five `include_router` calls (do not remove or reorder the existing five).
  </action>
  <acceptance_criteria>
    - `pytest backend/tests/test_linked_apps.py -x` passes all 3 tests
    - `grep -c "class LinkedModuleManifest" backend/src/modules/linked_apps/linked_manifest.py` equals 1
    - `grep -c "Depends(require_auth)" backend/src/modules/linked_apps/routes.py` is at least 1
    - `grep -c "linked_apps_routes" backend/src/main.py` is at least 2 (import + include_router)
  </acceptance_criteria>
  <verify>
    <automated>cd backend && /tmp/innkeeper-venv313/bin/python -m pytest tests/test_linked_apps.py -x</automated>
  </verify>
  <done>GET /api/modules/linked-apps/ is live, authenticated, returns an empty list; LinkedModuleManifest exists as a pure data shape with no behavior; main.py mounts the new router alongside the existing five without disturbing them.</done>
</task>

<task type="auto">
  <name>Task 2: LinkedAppsSection.svelte (empty-state card grid) and theme.css token audit</name>
  <files>frontend/src/lib/components/LinkedAppsSection.svelte, frontend/src/lib/api.ts, frontend/src/lib/styles/theme.css, frontend/src/routes/dashboard/+page.svelte</files>
  <read_first>
    frontend/src/lib/styles/theme.css (full current token set — confirm every token referenced by this task's new component already exists; do not invent new hex values), frontend/src/routes/dashboard/+page.svelte (existing card-grid + section layout pattern this new section slots into), frontend/src/lib/api.ts (existing apiGet/listDevices pattern to extend with a getLinkedApps() function), .planning/phases/05-plugin-system-notifications/05-UI-SPEC.md (exact copy strings: "No linked apps yet" heading, "Linked apps will appear here once a third-party module is configured." body, grid-template-columns: repeat(auto-fill, minmax(280px, 1fr)) with 24px gap, focal-point note that the empty-state heading uses page-heading-adjacent visual weight)
  </read_first>
  <action>
    Add `getLinkedApps(): Promise&lt;unknown[]&gt;` to `frontend/src/lib/api.ts` calling `apiGet('/api/modules/linked-apps/')`, following the exact `listDevices()` pattern (throw on non-ok response). Create `frontend/src/lib/components/LinkedAppsSection.svelte` using Svelte 5 runes (`$state`, `$effect` or `onMount`, matching the existing `dashboard/+page.svelte` style): on mount, calls `getLinkedApps()`, renders a `grid-template-columns: repeat(auto-fill, minmax(280px, 1fr))` grid with `gap: 24px` (the `lg` spacing token) when the list is non-empty; renders the empty state when the list has zero entries — heading text exactly "No linked apps yet" at 28px/700 (UI-SPEC's page-heading typography role, since this is the section's only content per the focal-point note) and body text exactly "Linked apps will appear here once a third-party module is configured." at 14px/500 in `--color-muted`. Import and render `LinkedAppsSection` inside `frontend/src/routes/dashboard/+page.svelte` below the existing device card grid section (do not alter any existing dashboard markup/logic — purely additive). Verify `theme.css` already contains every token this component references (`--color-surface`, `--color-fg`, `--color-muted`, `--color-border`); if any referenced token is missing, add it following the existing `:root`/`.dark` dual-declaration pattern — do not introduce a new ad-hoc hex value outside that pattern.
  </action>
  <acceptance_criteria>
    - `grep -c "No linked apps yet" frontend/src/lib/components/LinkedAppsSection.svelte` equals 1
    - `grep -c "Linked apps will appear here once a third-party module is configured." frontend/src/lib/components/LinkedAppsSection.svelte` equals 1
    - `grep -c "repeat(auto-fill, minmax(280px, 1fr))" frontend/src/lib/components/LinkedAppsSection.svelte` equals 1
    - `grep -c "LinkedAppsSection" frontend/src/routes/dashboard/+page.svelte` is at least 1
    - `cd frontend && npx svelte-check` reports 0 new errors introduced by this task's files
  </acceptance_criteria>
  <verify>
    <human-check>Open the dashboard in a browser (or run svelte-check) and confirm the "No linked apps yet" section renders below the device grid with the heading visually prominent (28px/700) and no layout break in the existing device-card grid above it.</human-check>
  </verify>
  <done>LinkedAppsSection renders the locked empty-state copy at the locked typography, reuses the exact device-grid CSS pattern, and is mounted on the dashboard without disturbing existing markup; svelte-check passes with no new errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser → GET /api/modules/linked-apps/ | Authenticated read-only endpoint; no user input accepted this phase (v1 ships no write path for linked apps) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | Information Disclosure | GET /api/modules/linked-apps/ | mitigate | Depends(require_auth) gates the route exactly like every other route in this codebase; no anonymous access |
| T-05-06 | Tampering | target_url rendered as a clickable link (future, v2) | accept | No real linked-app entries exist in v1 (empty list always returned) — XSS-via-malicious-target_url risk is deferred to whichever v2 phase first writes a real entry; out of scope per Scope Fence |
</threat_model>

<verification>
`pytest backend/tests/test_linked_apps.py -x` passes. `cd frontend && npx svelte-check` shows zero new errors. Full backend suite (`cd backend && /tmp/innkeeper-venv313/bin/python -m pytest`) stays green — this plan only adds files plus one additive `main.py` router-mount line.
</verification>

<success_criteria>
- `GET /api/modules/linked-apps/` is live, authenticated, returns `[]`
- Dashboard renders the "No linked apps yet" empty state using the locked UI-SPEC copy and typography
- theme.css remains the single token source — no new hex values introduced outside the existing `:root`/`.dark` pattern
</success_criteria>

<output>
Create `.planning/phases/05-plugin-system-notifications/05-02-SUMMARY.md` when done
</output>
