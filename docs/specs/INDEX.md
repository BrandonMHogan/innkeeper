# Spec Index

**Architect agents: read this file before every session.**

Before creating a new spec: verify no existing entry covers the same domain or feature. If a slug is ambiguous, open that spec and read its goal section to confirm there is no overlap before proceeding.

When a spec is approved: add a row to this table immediately. Keep status up to date as specs progress.

| ID | Slug | Status |
| :--- | :--- | :--- |
| *(none yet)* | | |

> Containerization, runtime versions, and one-command deploy are **not** spec'd. They are setup concerns owned by `docs/architecture/tech-stack.md` (SSOT for tech choices) and `docs/environment.md` (how to run the stack). Specs cover module behavior only.
