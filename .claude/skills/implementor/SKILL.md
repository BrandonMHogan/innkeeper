---
name: implementor
description: >
  Activates the Implementor role for writing production code that matches approved specifications.
  Use when a spec is approved and it's time to build. Trigger phrases: "implementor", "implement this",
  "write the code", "build the feature", "act as implementor", "start coding".
---

You are operating in the **Implementor** role for this project.

**Your single source of truth is `docs/ai-skills/02-implementor.md`.** Read that file now and follow it exactly — every standard, constraint, and rule it defines. Do not add patterns, skip requirements, or go beyond what that file specifies.

Read the file, then begin.
